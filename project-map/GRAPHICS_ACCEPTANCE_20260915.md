# 2026-09-15 地图验收阻塞记录

## 最终页面复验记录（03:30 左右，优先于后文）

- 60分钟墙钟巡检结束，页面记录291个采样、当时events=[]、errors=[]。采样存在约11分钟缺口，完整样本下载未取得，**连续60分钟稳定性不通过验收**，不报告无法核实的FPS/堆内存趋势。
- 最终退出补丁真实页面5次进入ready再退出已结束，cycle时间戳依次为1789460616557、1789460620874、1789460624699、1789460628661、1789460632729。结束后回到二维，无三维画布。
- 1789460743497发起受控WEBGL_lose_context。页面先显示停止资源队列与绘制，再重新创建画布并显示“基础画面初始化结束”。该项仅证明受控上下文恢复；**物理驱动故障后的恢复仍未验收**。未人为重置系统驱动。
- 复验后Windows System/4101最新仍为本地02:04:16 nvlddmkm；不代表将来不会崩溃。二维仍出现bicycle_parking、toll_booth、brownfield缺失sprite警告，不能宣称控制台完全无警告。
- 总部独立预览真实加载、拖动旋转、释放成功。画面仍偏暗，楼顶标识全楼视角辨识度不足；城区合批旧楼体尚未去重、其余项目参考资料未形成核实建模输入。故总部预览不计城区验收，**0/14**不变。
- 图形场景已退出/释放；没有改系统驱动设置、没有发布或提交Git。
- 最终源码统计：2417文件，184084352字节，175.56MiB，187553行（脚本固定口径）。44维完整新评估尚未形成，本轮不提升任何生产评分。

## 最新复验进展（本节覆盖后文旧测试数量）

- 根因修复：补充分块的纹理、网格和材质先由 Babylon 构造器注册到场景，又由 AssetContainer 重复注册，卸载后产生残留引用。`container-ownership.mjs` 转移唯一所有权；真实 Babylon NullEngine 连续20轮装卸验证无残留。
- 原城区目录读取增加超时/退出中止与晚到结果隔离；`original-city-exit.test.mjs` 验证退出后不能重建已销毁状态。
- 03:18 本机执行17个地图测试文件，40通过、0失败、0跳过。含预算拒绝、串行上传、晚到释放、恢复失败传播、总部几何/LOGO销毁及独立预览重复点击与加载中退出。不是全站或物理GPU验收。
- 02:24左右开始60分钟浏览器跨区巡检，进行中无记录到contextlost/error；03:18查询Windows4101最后仍为02:04:16。采样数量存在明显缺口，不能把墙钟计时当作60分钟连续活动渲染。
- 嵌入浏览器未返回Blob下载，当前运行的完整采样未取得；新QA导出增加只读JSON文本框作为降级路径，但不刷新正在运行的巡检来冒充同一轮测试。
- 总部完成独立A/B座、两连桥的照片参考几何与LOGO组件，不代表竣工精模。城区旧楼体仍在两个合批分块内，未做无依据裁切；当前依旧0/14完成城区接入。
- 本轮不承诺绝不崩溃；受控WebGL丢失不等于真实驱动重置恢复。最终退出复验、真实驱动恢复、总部可见外观和14项目接入必须分别记录。

## 本次资源预算更新（覆盖下文历史实现状态）

- resource-lane 现覆盖共享材质、初始化模型、原城区/补充区、山体及窗格的加载整合。单通道，384 MiB 估算预算，96 MiB 临时预留；不足先释放细节保留概览，仍不足拒绝加载。不是物理显存硬上限，驱动/着色器编译和解码峰值无法据此保证。
- context-lifecycle 统一拥有恢复事件，关闭 Babylon 自行重建，避免旧引擎销毁与延迟重建竞争。丢失时停止队列并中止 fetch，晚到资源释放；只尝试一次自动重建。初始化失败保持 rejected，不能报恢复成功。
- 默认基础光照；HDR 改为明确标记未验收的手动选项。空闲基础模式不再持续满速渲染。
- 修复立面插件在标准材质未启用 UV2 时仍读取 uv2 的编译错误，条件声明避免重复定义。
- 本轮地图相关 36 测试通过、0 失败、0 跳过；最终 ready 错误传播改动语法检查通过。浏览器基础模式短时达到 262 MiB、11 精细块、9 补充块、3 山体，道路就绪；刷新后的日志未新增原先 UV2 错误。已实测返回二维；这不等于物理显存释放量测定。
- 原版 HDR 测试仍于本地 02:04:16 出现 NVIDIA 4101；基础模式后的查询未发现新增 4101，但长期运行、真实设备恢复和反复跨区仍未验收。
- 安居精模仍为 0/14 验证接入，总部 LOGO 有资产/组件但未附着最终核实建筑。白三角根因、罗湖接缝不以本次预算修复替代验收。
- 本轮没有完整44维新证据，历史综合分不升级；生产稳定性仍未达标。

## 实际故障，不是完成声明

- 浏览器 WebGL 丢失：2026-09-15 04:40:00Z、06:28:45Z。
- Windows System/4101：对应本地时间 09-14 23:40:00、09-15 01:28:46，均报告 igfx 显示驱动停止响应并恢复；最近六小时另有三次同类事件。
- 显卡：Intel UHD Graphics（30.0.101.2079）、NVIDIA MX130（32.0.15.8183）。不能仅凭设备清单认定浏览器实际选用了独显。
- 查询时物理内存总量 16,576,504 KiB，可用 5,098,012 KiB。此单点不代表长期内存趋势。
- 当前返回二维，避免继续反复诱发系统驱动重置。未更改驱动、Windows 图形设置或正式服务启动方式。

## 本轮局部改动与证据

- scene-initialization：场景失效后停止后续初始化阶段。
- graphics-recovery：单次自动重建、重复事件隔离、视角快照恢复；不无限重试。
- resource-lane：原城区、补充区、窗格的动态模型解析上传共享单路；静态初始化和地形网格不在该通道内，不宣称所有 GPU 工作串行。
- district-ground：使用现存罗湖边界扣除水面形成 6,238 个三角面，583,261 字节；沿用原地表材质。它是平面补充地表，不是测绘精细地形；最终接缝仍未验收。
- missing-map-icons、sky-bounds、road-surface 等局部测试合计 39 通过、0 失败、0 跳过。只覆盖地图模块，不代表全站回归或长期稳定性。

## 必须继续，禁止历史结论覆盖

1. 白色三角尚未取得命中网格的诊断证据；远裁切/天空尺寸防护不能当作根因已修复。
2. 罗湖地表接缝和二维缺失图标的真实页面复验尚未全部完成。
3. 14 个项目精模接入仍为 0/14；候选清单、照片研究、LOGO 组件都不等于完成。总部 LOGO 尚未接到最终核实建筑。
4. 长期帧率、内存趋势、设备恢复与多次进出稳定性未通过。先获准切换到 NVIDIA 图形首选项并重启承载浏览器的应用，再验证；若仍重置，应独立排查驱动，不能修改系统 TDR 延时掩盖问题。

## 评估状态

沿用最近记录的能力覆盖 73.84、生产成熟度 74.92、数据资产 64.33，不调整任何分数。没有本轮完整 44 维复核证据，不能把这三个历史分数当作当前验收。优先项：驱动稳定性、可见几何缺陷、真实项目模型和来源核实。
