# 四区地形与安居精模实施状态

更新：2026-09-14（浏览器日志 UTC 2026-09-15）。状态：阶段实施中，尚未完成最终验收。

## 重开后的实际复验（以下记录优先于历史排查）

- 重开后 WebGL 恢复，曾实际显示腾讯地标和昼夜光照；不是一直 Disabled。
- 新一轮正式页面仍于 04:27:43 UTC 出现 context lost，04:27:46 报告 restored，但截图仍是异常色块。驱动恢复事件不等于画面恢复，不能据此宣布解决。
- 明确发现光照初始化把单采样改回 4 倍 MSAA，并同时使用两套 1024 环境光立方贴图。正式页面现显式采用 balanced 配置：两套降至 512、单采样配 FXAA，保留作者色调和光照；该项是资源预算修正，不是已证明的唯一崩溃根因。两套贴图每面的像素数各减少 75%，不等于整站内存减少 75%。
- 修改后正式页面重新显示建筑及道路材质；宝安概览可显示，原城区精细块 12→0、补充分块 9→0、山体细节 3→0，概览继续保留。长时稳定性、显存、帧率仍未验收。
- 后续切换罗湖全貌并退出二维、重新进入三维，实际画面恢复且道路材质就绪；本次复入未发现新的 context lost。罗湖概览仍有明显边界，不能算画面统一验收通过。二维新增观测到 gate/swimming_pool/bollard/cycle_barrier 图标缺失警告，底图资源完整性仍需修复；没有将这些警告归因于三维 GPU 故障。
- `detailed-city.mjs` 已接入 61 块地形流式模块，附近最多 9 个细节、并发 2；此项覆盖下文历史“未接入”的记录。
- `road-surface.mjs` 接回原道路分块遗漏的作者沥青法线/ORM材质，所有活动分块共享两张贴图，实际页面显示“道路材质 就绪”。没有新增实时反射渲染目标。
- 快速下降在纵向移动完成后再次按地表高度校验，避免 Q 快速下降越过地面；模块行为测试通过。
- 分块失败提示区分超时、HTTP 与解析失败；未确定此前某次暂态读取失败的唯一原因。诊断页已补齐四区目录，但补齐后的七阶段尚未全部重新执行。
- 本轮地图局部行为测试：24 通过、0 失败、0 跳过；相关修改 JavaScript 语法检查通过。不是全站回归，也不是长时 GPU 稳定性通过。
- 14 项逐栋精模完成仍为 **0/14**，总部模型及屋顶 LOGO 尚未接入。不能拿通用模型替代已核实实景建筑。道路实际坡度、水面材质的完整迁移仍未完成。

## 以下为重开前历史记录（不代表当前接线状态）

## 本轮继续排查

- 隔离页面 `gpu-diagnostic.html` / `gpu-diagnostic.mjs` 将模型加载拆成七个手动阶段；当前在空场景创建阶段即失败，未进入任何模型加载阶段。
- 最低 WebGL1、无抗锯齿/模板缓冲配置，localhost 和 127.0.0.1 均返回 `GL_RENDERER = Disabled`、`BindToCurrentSequence failed`。这是当前浏览器图形上下文不可用的证据，不足以证明最初故障原因。
- 显卡枚举：Intel UHD Graphics、NVIDIA GeForce MX130（报告适配器内存 2 GiB）；没有修改驱动、安全参数或正式服务。
- 用户 LOGO 原件已保存为 `models/anju-logo-user.png`；`anju-rooftop-sign.mjs` 保留图片比例，要求父楼体与局部坐标，释放父模型时释放标牌资源。未接入未经核实的楼体；不计作 A 座精模完成。
- 浏览器图形环境恢复后，先通过空场景，再逐阶段诊断；当前禁止继续叠加资产并宣称页面验收通过。

## 已保留的独立成果

- `scripts/fetch-city-dem.mjs`：两幅公开 Copernicus DSM 数据下载与大小、SHA256 校验；原始资料总计 61,671,503 字节。
- `scripts/build-city-relief.py`：宝安、福田、南山、罗湖地形生成；既有道路、水面、建筑轮廓保留平面衔接走廊。
- `city-relief-v1/`：61 个非空细节块及概览、清单，合计 1,271,222 字节。
- `relief-core.mjs`、`city-relief-stream.mjs`：概览保留、附近最多 9 个细节块、并发 2、远离释放、失败重试。当前没有接入 detailed-city.mjs。
- `tests/city-relief.test.mjs`：4 项行为测试；与 project-observer 合跑共 8 通过、0 失败、0 跳过。

这不是测绘级裸地高程；道路仍保留原有标高，不代表已经校正真实坡度。新材质不是作者全部草地、水面着色器的完整移植。

## 实际浏览器阻塞

新地形接入后发生 WebGL context lost，自动恢复后画面仍空白。已仅撤销本次地形接线，未回退用户其他改动。

关闭测试标签并新建标签后：在线二维底图正常；不含新地形的原有三维仍再次出现“图形设备暂时中断，正在恢复”，画面空白。这说明不能将问题直接归因于新地形，也不能把模块测试通过等同于页面通过。未改动浏览器安全或 GPU 设置，未重启正式服务。

## 尚未完成

1. 三维基线稳定性故障定位；恢复后再测试新地形接入、帧率、长时内存和分块释放。
2. 四区原作者级草地、贴地道路、水面视觉升级。
3. 14 个安居候选项目逐一核验建筑照片、位置、现状/规划属性并建模：当前完成 0/14。候选清单不是已完成模型。
4. 深圳湾创新科技中心 2 栋 A 座精模和安居 LOGO：未完成；须区分用户要求的屋顶标志与已核实的现状标志，不能用腾讯大厦替代。

## 评估口径

本阶段未验收，不提高实现或生产成熟度评分。沿用此前基线：能力覆盖 73.84、生产成熟度 74.92、数据资产成熟度 64.33；本记录没有独立重评全项目。优先级：恢复三维基线、完成四区地形视觉验收、逐栋核实参考资料后制作精模。
