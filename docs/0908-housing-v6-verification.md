# 住房改造 V6 逻辑替换记录

## 2026-09-08 表模纠偏补充（以下结论优先于原记录）

用户指出上一版把表头与合并结构过度简化，此问题属实。此次从同一DOCX提取真实行列，形成可复现的脱敏源结构快照 `data/report-table-housing-v6-source-schemas.json`，由表库构建器消费，避免继续手工拼造源表。

- 26套表模中21套映射原Word实体表；另外5套依据经理文字要求设计，后台明确标注无实体源表。
- 保留原表表头、列宽比例、横纵合并、固定科目和汇总行；仅清除项目样例事实。短明细表扩为4个填数行，原有较长模型不裁短。
- 所选案例租金汇总表恢复8列、3个案例行、原加权汇总行与整行合并结论；示例32元改为待填，不删除计算模型。
- 修复3.6.2标题数字粘连导致排序异常、表格第七章排在第十一章之后、表模与逻辑版本混称、市场子规则重复父节全套说明四项遗留。
- 住房逻辑4.0，本机发布记录v7，表模基线v4。数据库没有任何表模覆盖记录；住房迁移前后商业有效规则一致，重复初始化不新增版本。

验证：定向36项通过；全量隔离PostgreSQL回归689项通过、0失败、0跳过（`outputs/0908-table-fix-tests-final.log`）；随后补充的真实Word结构测试连同表模模块14项通过。后一次只增加测试，未改公共实现，因此未重复全量回归。Word压缩包内document.xml核验8列合并及原加权公式；页面渲染器验证6行。

正常网络服务已重启：Provider网络可达，站点、项目API、RAG及真实AI生成均200；临时测试账号与隔离数据库均已清理。用户解锁后台后实际点开住房逻辑与第三章表库，显示4.0/v4、章节顺序及完整修正表头、公式、结论；未写入管理员自定义表格。尚未对真实项目新稿逐页做Word视觉验收，也未模拟本次浏览器断网失败态，不能把自动化通过等同于真实报告已经验收。已有报告正文和原Word均未覆盖；未提交Git。

主要改动责任文件：`report-table-admin.js`、两份住房表模JSON、住房逻辑JSON、`scripts/build-housing-v6*.mjs`、`scripts/extract-report-logic-review.py`及运行时种子/发布清单；定向测试覆盖表模、章节排序、版本和迁移。

评分沿用本日基线：44维覆盖73.84、60维覆盖74.30、生产成熟度74.92、真实资产代理64.33（不等于经正式验收的资产分）。本次补齐26/47/49/52/58维证据，不提分；其余维度无变化，未重新专项评定。优先三项：真实项目新稿逐表核对；经理确认5套文字派生表；正式Word分页与来源证据验收。

源码统计：482文件，11,093,839字节，10.58MiB，101,998行。遵守固定排除口径。

日期：2026-09-08。范围仅住房改造逻辑及其表格，不包含其他历史需求。用户原始 Word 不修改，已有报告正文不自动覆盖；未提交或推送 Git。

## 实际更新

- 根据原件红字及行内经理批注提炼规则：简洁正式表达、动态章节编号、全部房源按主题统一分析，禁止主次两套论述及 POI 代理论证。示例数字不能成为项目事实，口径冲突必须待确认。
- 第一章压缩重复，补齐物业现状、改造前后、合规、合作单位和投资表格职责；页数是排版目标，不宣称提示词保证页数。
- 第三章按宏观经济、人口就业租赁需求、职住产业、目标客群、住房租赁供给、竞品租金、供需结论、产品定位排列八节。
- 住房场景表格由14个扩展到26个，保留表头、单位及填数位置，不导入样例数值。第四章统一房源分析，第五章消费第三章市场定价结果。
- 修正 report.js 中旧的“重点点位后分析其余点位”策略，避免实际生成提示抵消全篇要求。
- 按住房场景合并升级，保留商业场景与未涉及的自定义规则。业务逻辑版本3.0；本机发布记录版本6（两者含义不同）；旧记录版本5保留。

## 责任文件

规则：data/report-logic-housing-v6-review.json、data/report-logic-gaibao-v1.json；表格：data/report-table-templates-gaibao-housing-v1.json。

构建：scripts/build-housing-v6.mjs、scripts/build-housing-v6-tables.mjs、scripts/generate-reportlogic-runtime-seed.mjs；运行时种子及发布清单同步生成。

发布兼容：functions/api/_reportlogic-scoped-migration.js、functions/api/reportlogic.js；生成策略：report.js。对应测试覆盖规则、版本、表格和迁移。

## 已执行验证

- 模块检查27项通过；隔离 PostgreSQL 全量回归685项通过，0失败、0跳过。日志 outputs/0908-v6-regression-final.log。
- 本机发布版本5与6的商业场景经标准化后相等；重复初始化不新增版本。本机没有表格覆盖版本，旧单元格覆盖不会错套到新表。
- 服务按正常网络权限重启，默认 Provider 网络可达；站点、项目接口、RAG及真实AI生成均返回200。临时测试账号已清理。
- 浏览器目前停留在管理员安全验证，已请用户自行解锁；本轮尚未完成后台点击、刷新及真实项目新稿的页面验收，不据自动测试宣称该部分完成。

## 评分与边界

沿用用户提供的0908 f9664ba复评基线及 docs/0907-three-fixes-verification.md：44维73.84、60维74.30、生产成熟度74.92、真实数据资产代理64.33，真实项目单项52。本轮不提分或降分。直接补充26提示词版本、46专业性、47规则治理、49表格、52范围发布、58一致性证据；其他维度未重新专项核验，原证据及缺口继续保留。未完成新的正式项目独立复核，不把新增表格数量当作资产成熟度。

后续优先：1. 解锁后台核验3.0与八节排序；2. 用授权项目材料生成新稿，逐表核对缺失值、来源和编号；3. 经理确认新稿表达及正式 Word 排版。不会为了这些验证自动重写现有工作稿。
