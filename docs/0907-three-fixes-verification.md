# 1—3 项修复技术验收记录

日期：2026-09-07（本机时区）。适用版本：f9664ba3a47455e9ac557a222dabf77b3a291a95 上的本次未提交工作树。

## 范围及数据边界

只处理用户指定的正式签发与导出、检查覆盖范围、保存与阶段一致性及私人方案交接；评估文档其他方向未实施。未提交或推送 Git。未改变真实报告正文、未代用户冻结或签发。独立复核仍要求有效只读成员且无本项目编制权限历史。

## 实现与责任文件

- `app.js`、`report.js`：旧签发入口转入独立冻结复核，不再在前端设置正式标记。
- `functions/api/_delivery-snapshot.js`、`_delivery.js`、`report-delivery-ui.js`、`export.js`、`docxgen.js`：V3 快照包含项目身份、正文、测算与引用；重新检查历史评分；正式 Word 使用批准快照及其日期、文号，不使用当前工作稿。V1/V2 保留读取兼容，但不得借用当前身份导出正式版，需重新冻结。
- `functions/api/_report-quality.js`：区分已声明检查与数值覆盖；无核验依据或未覆盖金额不得给出总检查通过/100；自动检查不代表事实正确或人工复核完成。
- `functions/api/_investment-lifecycle.js`、`projectbrain.js`、`investment-lifecycle.js`、`project-brain.js`、`project-intelligence.js`：空成果、缺来源版本、过期或变更证据不能作为有效闭环依据；失效实际值不进入可比较汇总；过期事实不计为健康已确认。
- `auth.js`、`project-manager.js`：保存返回 false 或超时阻止导航；列表与工作区读取同一工作阶段。
- `functions/api/projects.js`、`projectworkspace.js`、`_investment-ops-service.js`、`investment-ops.js`：阶段权威读取；编辑者私人方案提交、负责人退回/采纳、重复提交防护及事件审计；非提交私人方案不向其他人暴露。

## 实际验证

- 隔离 PostgreSQL 全仓回归：683/683 通过，0 失败、0 跳过；包含迁移空库、重复执行、升级和多人方案提交/退回/采纳。临时测试库已删除。
- 变更 JS/MJS 语法检查及 git diff --check 通过（只有换行格式提示）。
- 最终本地服务 PID 5516 重启前运行中任务为 0；启动确认默认 AI Provider 网络可达。
- 同一最终服务：站点、项目 API、RAG query、真实 `/api/generate` 均返回 200。专用非登录测试账号已删除，未操作真实项目；服务侧测试调用日志可能保留。
- 真实浏览器：项目列表与工作区均显示“初步研判”，刷新后项目及阶段恢复，未见新增控制台错误。
- 隔离浏览器使用实际 export.js/docxgen.js 生成真实 DOCX；XML 验证批准正文、100万元和冻结文号存在，当前工作稿名称及99999值不存在。旧快照被阻止正式导出。
- 隔离浏览器使用实际 navigationGuard：保存 false 时保留当前页，重复操作同样阻止切页。

## 未验证边界

- DOCX 已做实际构建和 XML 内容检查；当前运行环境未找到 LibreOffice，未完成逐页渲染及 Microsoft Word 排版验收。本轮不宣称所有 Word 版式已验收。
- 多人交接已由真实 PostgreSQL 行为测试覆盖；未安排业务人员用多个真实账号完成正式项目签发。本次未提升真实项目成熟度。
- 自动数值覆盖不等于全报告语义正确或政策效力正确；仍需独立人员审查。

## 44/60 维证据复核（复用本轮用户提供基线）

继承 `0908_AIStudyReport_f9664ba_方案落实与60维复评 (2).md` 的完整 60 维（前44维为核心）及权重；不再混用仓库旧71.22基线。60项分数本次均标记“无变化”，只追加本次代码/测试证据；没有用补丁行数或测试通过率换算增分。

|指标|沿用分数|本轮变化|
|---|---:|---:|
|44维核心能力均分|73.84|0|
|60维能力证据均分|74.30|0|
|生产成熟度加权分|74.92|0|
|真实数据资产成熟度代理分|64.33|0|
|真实项目验证单项|52.00|0|

直接改善的证据项：人工审签、数值可解释性/证据追溯、交互恢复、项目协作、单一事实源、生命周期；未新增下降项。其余维度未专项重验，沿用原缺口与动作。上述覆盖分不是生产可用率，真实资产代理分不是已拥有真实资料的比例。

后续优先：①业务人员独立复核并在 Word 中验收一个真实冻结成果；②真实多账号完成提交、退回、采纳与撤权场景；③对正式项目的金额核验范围和过期资料进行业务确认。以上不是本次擅自扩大的开发任务。

源码统计（含两个隔离验证脚本）：473文件，10,563,937字节，10.07 MiB，83,712行；口径见 scripts/count-source-code.mjs。
