# 300同时在线 / 1000日活容量准入（2026-09-10）

状态：**4核8GB独立环境的300用户、10分钟应用层混合准入已通过；尚未生产认证。** 未部署远程服务器，未改变现网连接池或放开AI并发。该结论覆盖本次明确的浏览、编辑、分块保存、128KiB上传和真实LibreOffice排版混合负载，不等于300个真实浏览器、300个外部AI任务或1000日活生产认证。

## 300用户持续混合复测结论（2026-09-10）

独立环境：Ubuntu 24.04.4 LTS、4 vCPU、8328425472字节（约7.76GiB）内存、无swap；300个独立账号，43小节/账号，思考时间5秒，持续600秒。排版使用真实LibreOffice和9968字节DOCX夹具，未调用外部AI供应商。测试数据库、账号和研究均由脚本创建，最终逐份校验后自动删除；正式数据库0写入。

|业务|请求数|成功率|p50|p95|
|---|---:|---:|---:|---:|
|浏览|22,834|100%|12ms|149ms|
|编辑准备|10,567|100%|20ms|172ms|
|保存|10,567|100%|21ms|169ms|
|上传|1,407|100%|75ms|323ms|
|排版导出|351|100%|1,454ms|3,764ms|
|结束逐份校验|300|100%|11ms|21ms|

合计45,726次业务请求，0失败；300份报告最终内容全部一致。整台虚拟机CPU平均39%、峰值83%；内存峰值1,832,517,632字节，占可用内存22.00%；Node RSS峰值344,481,792字节。

与同一脚本首轮失败基线相比：排版拒绝90次降至0次（消除100%）；浏览p95从479ms降至149ms（下降68.9%），编辑准备从515ms降至172ms（下降66.6%），保存从515ms降至169ms（下降67.2%），上传从864ms降至323ms（下降62.6%），成功排版p95从11,530ms降至3,764ms（下降67.4%）；Node RSS峰值从349,437,952字节降至344,481,792字节（下降1.42%，可视为基本持平）。这些是同一应用层压测脚本的前后对比，不表述为整站或用户电脑提速。

原始机器可读结果：`outputs/0910-http-capacity-result.json`；失败基线：`outputs/0910-http-capacity-baseline.json`。

## 用户确认的部署基准

> 最新调整：用户要求以 **4核、8GB** 为主要优化和最终验收基准，替代下文原8核16GB申请档。300在线/1000日活仍是待验收目标，不能套用16GB本机成绩。2026-09-10本机采样：4物理核/8逻辑处理器，CPU16%，总内存16576504KiB、空闲6173948KiB；各进程工作集之和不等于单个网页占用，且可能包含共享页。当前未在用户工作机施加8GB硬限制。

本轮只读小节接口已接入身份、研究授权、轮次和版本校验；隔离PostgreSQL真实行为测试覆盖未登录、跨账号、旧版本、错误轮次及撤权。带research-slice-v1标记的切片不能送完整保存接口，原稿不变。这不是任意局部状态自动合并协议：前台仍使用完整状态，尚未完成页面按需加载，不得把未加载字段当空字段保存。

直接/api/generate已启用2执行/8等待；Linux排版/api/report-pagination为2执行/8等待、最长等待30秒，Windows排版保持1执行/8等待；OCR单独1执行/3等待、最长等待15秒。超量429、等待超时503，均有重试提示。流式生成到流结束/取消才释放名额。当前仍是进程内有界排队，不是重启后可恢复的持久任务队列，也不等于放开300个AI或排版并发。

2026-09-10重启后已安装独立WSL2环境`StudyReport-Capacity`，Ubuntu 24.04.4 LTS。官方镜像391541571字节，SHA256为`9b2f7730dc68227dd04a9f3e5eab86ad85caf556b8606ad94f1f29ff5c4fd3f5`，下载后校验一致。运行态`nproc=4`、`MemTotal`约8.33GB、Swap=0，40GB动态磁盘。用户级`.wslconfig`设置memory=8GB、processors=4、swap=0，关闭localhostForwarding和GUI。Node 24.18.0、PostgreSQL 16、pgvector和LibreOffice 24.2.7依赖已完成，并已跑满上述10分钟混合压测。WSL与Windows共享物理资源，4个虚拟处理器不等于独占4个物理核，不能宣称等同独立生产服务器。未复制正式数据库、账号或密钥。

依据用户提供的服务器申请截图，规划按Ubuntu Server 22.04 LTS或24.04 LTS、优先8核（4核为降级环境，须另测）、16GB内存、100–200GB硬盘、固定内网IP、SSH 22端口和sudo管理权限、内网8080服务端口执行。这是目标配置，不是已经取得或已验证的服务器。

容量目标仍为300同时在线操作、1000日活，不等于300个AI任务同时执行。本机8逻辑处理器的Windows短测不得替代8核Ubuntu最终验收。带宽、磁盘性能、数据库最终位置和模型服务吞吐尚未提供，不猜测已满足。

默认规划业务应用、PostgreSQL和本地bge-m3向量服务共用该机器的资源预算；这是保守规划假设，部署前核实。16GB不能全部分给Node或数据库；向量化、解析、排版分别限量调度。硬盘容量同时计算原件、数据库、索引、临时文件、日志和备份，不全部当可用材料空间。规划初验以200GB档、保留20%空闲为目标，100GB档需单独验证容量及保留策略；不自动删除用户资料。

截图要求数据不流出单位/云端：作为目标部署安全边界，生产环境不得默认沿用开发机的外部AI、嵌入或解析服务。上线前核对单位授权的模型接口及出网策略；目前开发服务尚未改为单位内网部署，不能宣称已经满足该安全边界。截图底部模型条款不完整，不能据此假定具体模型或额度。

已知配置足以继续本地按需加载与队列改造，不以缺少远程登录信息阻塞这些工作；实际发布和供应商容量验收待目标环境具备后执行。不读取或要求用户在聊天中提供密码。

## 已测基线

### 4核8GB WSL2首轮存储基线（2026-09-10）

独立Node.js 24.18.0安装在`/opt/studyreport-runtime`，不替换Windows运行时或Ubuntu系统Node。项目测试副本位于`/opt/studyreport-capacity`；依赖按lockfile安装56个包，PostgreSQL16与pgvector0.6.0启动正常。首次建库因缺少vector扩展失败，临时库已清理；补齐扩展后重跑通过。

执行`.tmp/run-capacity-baseline.sh`：36项迁移文件的空库、重复执行、升级检查通过；研究权限/版本切片保护、切片读取、排队流生命周期和存储负载共7测试通过、0失败、0跳过。临时库`studyreport_restore_1789040327915016026`已自动清理。

|并发账号|全部流程总耗时|账号流程p95|错误|
|---:|---:|---:|---:|
|100|1894ms|1751ms|0|
|300|6745ms|6629ms|0|

每账号创建独立研究，43小节与合成材料，分块暂存、保存、重复请求重放、完整读取并逐值核对。单轮合成存储测试，不包含HTTP、真实浏览器、AI、材料上传或排版；约8.8秒短测不证明300人持续使用。与下文Windows基线的OS/运行环境不同，不将差值宣传为优化提速比例。尚未完成100/200/300用户混合持续压测；完整页面按需加载和持久化重任务队列亦未完成。能力评分沿用，不因安装成功或单次短测提高。

本机8逻辑处理器、16,974,340,096字节内存。独立PostgreSQL测试库，100/300个独立账号同时创建研究、分块暂存、提交、重放、完整读取校验。

|账号数|全部流程总耗时|单账号流程p95|错误|
|---:|---:|---:|---:|
|100|3203ms|2535ms|0|
|300|8037ms|7801ms|0|

日志.tmp/storage-300-baseline.log。测试库自动删除，正式数据库无测试写入。流程p95不是单次HTTP延迟，不含真实浏览器/网络/AI/导出，不证明300人操作流畅。300账号约7.8秒的流程p95需继续优化。

复现：设置STORAGE_CAPACITY_300=1，再运行node --env-file=local-server/.env scripts/run-postgres-tests.mjs tests/storage-load-postgres.test.mjs。该模式显式启用，默认回归仍10/30/50，不给每轮测试额外300账号负担。

## 实施顺序与验收

1. 按上方已确认硬件基准推进；最终部署前核实带宽、磁盘性能、数据库位置及单位模型接口。AI/导出并发以有界队列梯度实测确定，不把未知的峰值人数当作无限并发。1000日活不能独立推算每秒请求数。
2. 超大研究拆首屏、章节、材料、历史加载。部分加载状态不得被完整保存接口当成全量覆盖；需版本令牌及服务端合并，导出/生成取完整授权数据。旧JSON记录保持兼容；未取回内容不允许误显示为空或删除。
3. 普通请求与AI/解析/排版隔离；重任务有有界并发、排队上限、超时、取消、重试和可见状态，不靠无限连接池堆积。总连接数按所有服务进程计算。
4. 独立HTTP压测环境分100/200/300用户梯度，混合浏览、编辑、增量保存、材料上传、队列任务和导出，含思考时间及真实体积。外部AI额度成本另核准；替身任务测试必须明确不是实际供应商容量。
5. 建议待确认门禁：300在线持续60分钟，业务错误<1%、无串写/丢失；普通读取p95<1秒、增量保存p95<2秒；记录CPU、内存峰值、事件循环延迟、数据库等待、队列等待及结束后内存回落。阈值为目标，不是已达成结果。
6. 在最终硬件/网络上复验，验证备份恢复、服务重启和过载拒绝。确认回退读取器兼容后才部署。每日1000用户的存储增长和模型预算按实际资料量/任务数计算，不宣称一次短测证明日活容量。

## 评估与边界

沿用当日44/60维基线73.84/74.92/64.33，无加减分；新增300账号合成存储证据不提高生产成熟度。最优先：按需加载、重任务隔离、最终服务器混合持续压测。本轮仅增加显式容量测试入口及文档，未修改业务保存协议，无须重复全仓回归；定向数据库测试1通过0失败0跳过。
