# 2026-09-10 用户要求暂停：明日从此处继续

> 续接结果：已于2026-09-10按本记录继续并完成第二轮300用户600秒复测。45,726次业务请求0失败，351次排版全部成功，300份报告逐一一致；独立PostgreSQL回归891通过、0失败、0跳过；正式服务已按当前代码恢复，真实生成、项目接口与浏览器新标签验收通过。定量结果以`CAPACITY-300.md`和`outputs/0910-http-capacity-result.json`为准。下文保留暂停时现场记录，不再代表当前状态。

用户要求先暂停，不自动预约、不后台继续压测；不使用子Agent，不提交或推送。正式Windows8080网站保持运行。

## 已有证据

- `aireport.js`报告预览按视口展开正文，修复被长容器撑高导致43节全展开的问题（IntersectionObserver root=null）。真实龙岗研究43节首次进入正文仅展开2节，跳末章再展开；43份候选稿保留。刷新、关闭再打开验证；浏览器错误日志为空。只是DOM按需渲染，研究数据仍完整获取，不能称网络按需加载已完成。
- 直接生成与agent-worker共享2执行/8等待预算，Worker先取得名额再领租约；流式响应结束才释放。
- 13项针对性测试通过0失败0跳过（report-output-policy、report-preview-lazy、agent-worker-admission、work-admission）。当前版本全量和独立数据库完整回归尚未执行。
- 独立WSL StudyReport-Capacity，4逻辑核、8328429568B内存、无swap；Node24.18.0、PG16、LibreOffice24.2.7。正式库/密钥未复制。
- 100用户60秒HTTP浏览/保存/上传/实际排版混合测试：0错误，100份状态最终核对一致。
- 300用户600秒第一轮：浏览22204次p95=479ms、保存10261次p95=515ms、上传1369次p95=864ms，全部成功；排版343次中253成功90次429。300份最终报告逐值一致。Node峰值349437952B。此轮失败，不认证容量。详细记录：outputs/0910-http-capacity-baseline.json。

## 已调整但尚未验收

- Linux documentAdmission从1执行/3等待/15秒改为2执行/8等待/30秒；Windows保持1执行。OCR单独1执行/3等待/15秒，避免共享OCR实例并发。
- report-pagination.js仅Windows保留busy互斥，Linux使用独立临时目录及LibreOffice用户配置，由HTTP入口限制并发。
- 压测增加整台VM CPU、内存采样和即时错误明细。
- 第二轮300用户600秒在用户要求暂停后，准确终止WSL测试子进程28741和测试服务28752，外层脚本已删除临时库studyreport_restore_1789041963560028715。测试因人工暂停显示失败，不是有效复测成绩。第一轮库也已自动清理。
- Windows8080服务PID10652，exec会话59961，未停止。它仍是排队调整前运行版本；明日最终验收前需正常网络权限重启，完成真实generate及相邻API冒烟。不要把磁盘代码更新当运行态已更新。

## 下一步顺序

1. 检查git状态及服务进程，保留未提交用户修改；确认无残留测试排版进程。
2. `node .tmp/prepare-capacity.mjs` 更新纯代码快照，再执行 `wsl.exe -d StudyReport-Capacity -u root -- bash /mnt/c/Users/HP/Desktop/ai-studyreport-local/.tmp/run-capacity-http.sh 300 600`。这些临时脚本已在仓库.tmp，测试创建/删除自身独立数据库。保存完整日志，核对CPU、内存、排版拒绝和300份最终内容。
3. 通过后补持续/长报告/恢复测试及全量 `npm run test:integration`，0失败0跳过；不能把小样页数核验称完整长报告下载或300真实浏览器。
4. 网络级按需读取仍需页面适配与局部保存保护；不得把未加载字段当空数组覆盖。文档重任务目前仅进程内有界排队，不是持久化队列，用户可见排队状态仍需完善。
5. 更新CAPACITY-300.md、44/60维证据和源码统计，正常权限重启正式服务并验收。尚不宣称300在线/1000日活生产达标。

## 暂停时评估和统计

44/60维沿用当天基线：覆盖73.84、生产成熟度74.92、资产代理64.33，无加减分。最优先：排版复测、网络按需加载/保存保护、持续混合与恢复验收。

源码统计：618文件，12000683字节，11.44MiB，113458行（固定脚本口径，不含outputs和.tmp）。本记录为技术续接说明，不是完成交付。
