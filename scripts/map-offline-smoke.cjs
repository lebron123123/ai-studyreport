const {chromium}=require('C:/Users/HP/.cache/codex-runtimes/codex-primary-runtime/dependencies/node/node_modules/playwright');const assert=require('node:assert/strict');
(async()=>{const b=await chromium.launch({headless:true,channel:'msedge'});try{const p=await b.newPage(),external=[],errors=[];p.on('pageerror',e=>errors.push(e.message));await p.route('**/*',r=>{const u=new URL(r.request().url());if(!['127.0.0.1','localhost'].includes(u.hostname)&&u.protocol.startsWith('http')){external.push(u.href);return r.abort();}return r.continue();});await p.goto('http://127.0.0.1:8080/project-map/index.html');await p.waitForFunction(()=>document.querySelector('#status').textContent.includes('本地离线简版'));assert.equal(await p.locator('#basemap-mode').inputValue(),'offline');await p.locator('#map').hover();for(let i=0;i<5;i++)await p.mouse.wheel(0,-500);await p.waitForTimeout(3000);await p.screenshot({path:'outputs/map-offline.png'});await p.locator('#nearby').click();await p.waitForFunction(()=>document.querySelector('#map').dataset.details==='ready',null,{timeout:180000});await p.locator('#city').click();await p.waitForFunction(()=>document.querySelector('#status').textContent.includes('本地离线简版'));await p.reload();await p.waitForFunction(()=>document.querySelector('#status').textContent.includes('本地离线简版'));assert.deepEqual(external,[]);assert.deepEqual(errors,[]);console.log('PASS external blocked: 2D load, five zoom operations, 3D load, return, reload; no external requests, no page errors');}finally{await b.close();}})().catch(e=>{console.error(e);process.exitCode=1;});
