# 第三步：权限与数据隔离（密级三级+部门）真正的部署包

## 抱歉：上次说"已完成"但没有交付文件，是我的失误
上次核实时我验证的是本地草稿，没有打包给你、你也没上传部署，所以线上网站没有变化。这次是真正要上传的文件。

## 改动的文件（2个）
| 文件 | 传到哪里 |
|---|---|
| `admin.html` | 仓库**根目录**替换 |
| `rag.js` | **functions/api/** 目录替换 |

## 数据库（你已经执行过了，不用重复）
```sql
ALTER TABLE users ADD COLUMN department TEXT DEFAULT '';
ALTER TABLE users ADD COLUMN clearance INTEGER DEFAULT 1;
ALTER TABLE rag_files_v2 ADD COLUMN security INTEGER DEFAULT 1;
ALTER TABLE rag_files_v2 ADD COLUMN dept_scope TEXT DEFAULT '全部门';
```

## 部署后你会看到什么（验证清单）
1. RAG知识库页面，检索测试上方多一行"👥 用户权限"链接——点开能看到用户列表，每行"设置"可填部门+密级(1公开/2内部/3涉密)
2. 上传文件区，分类/权威等级旁边多出"密级"下拉框 + "可见部门"输入框
3. 已入库文件列表，每个文件多一个"权限"按钮，可事后改密级/可见部门
4. 检索结果自动按登录用户的密级+部门过滤，管理员豁免可见全部

## 功能说明
- 密级三档：1公开(全员)/2内部/3涉密——用户能看的文档密级必须 ≤ 自己的密级权限
- 部门限定：文档可设"可见部门"（如"投拓部"），非该部门+非管理员看不到；留空/"全部门"=不限
- 双重过滤：密级和部门同时满足才可见
