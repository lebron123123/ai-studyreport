# 项目协作与变更准入规则

本文件是本仓库所有代码修改的默认工程约束。目标是：改动尽可能小、模块先自测再集成、问题可定位、结果可回退。除非用户明确要求，否则只在本地修改，不提交或推送 Git。

> 边界说明：本文件只存放项目协作与变更规范。Agent 原理、Hermes 学习、核心代码解读和升级路线统一维护在 `outputs/0826Agent学习总成.md`，不得把教程内容混入本规范。

## 1. 基本原则

1. 先确认现状，再修改。开始前检查 `git status --short`，用户已有改动不得覆盖、清理或顺手重构。
2. 一次只解决一个清晰问题。优先修改责任模块，不跨模块复制逻辑，不借机改名或格式化无关文件。
3. 优先最小补丁。能修一处入口就不同时包装多个入口；能补兼容层就不破坏旧数据。
4. 模块必须先通过自身检查，才允许接入公共入口、导航、数据库或导出链路。
5. 测试强度按风险选择，小改动不机械运行全部测试；但计算、导出、鉴权、数据库和跨模块工作流必须做完整回归。
6. 最终交付必须说明：改了什么、涉及哪些文件、做了哪些验证、仍有哪些边界；不得把“代码已写”当成“功能完成”。

## 2. 主要模块边界

- 页面导航与登录恢复：`app.js`、`auth.js`、`ui-route-state.js`。
- AI PPT 对话与流程：`ppt-conversation-*.js`、`ppt-agent-workflow.js`、`ppt-guided-flow-v2.js`。
- PPT 页面设计、预览与导出：`ppt-components.js`、`ppt-design-ir.js`、`ppt-export*.js`。预览与导出必须消费同一份设计数据，不得维护两套视觉逻辑。
- PPT 素材中心：`ppt-asset-center.*`、`ppt-asset-admin.js`、`functions/api/pptassets.js`。
- 可研生成与复核：`report.js`、`review.js`、相关 `functions/api/`。
- 财务测算：`nrcalc.js`、`rentcalc.js`、`salecalc.js`、`investestimate.js`。公式修改必须有数值回归测试。
- 本地服务与存储：`local-server/`、`migrations/`。数据库变更必须向后兼容并可重复执行。

若一个需求同时影响多个模块，先写清输入、输出和责任模块，再按“底层模块 → 适配层 → 页面入口”的顺序集成。

## 3. 每次变更的准入流程

### A. 基线核查

- 检查工作区状态，识别用户未提交改动。
- 找到功能唯一入口、数据来源和现有测试。
- 记录修改前可复现现象；若基线本身失败，先报告并区分旧问题与本次问题。

### B. 模块内实现

- 只修改必要文件；新增能力优先放在职责明确的独立模块。
- 公共接口保持最小，只暴露调用方需要的方法和数据。
- 不允许隐式修改其他模块的全局状态。
- 本项目大量脚本由普通 `<script>` 加载，共享全局作用域：新增常量、函数必须使用模块前缀或 IIFE，避免同名声明导致整文件失效；`index.html` 的脚本顺序视为接口契约。

### C. 模块自检

模块未通过以下检查，不得汇入主流程：

- JavaScript 语法检查：`node --check <变更文件>`。
- 对应单元/回归测试；优先验证行为和输出，不以字符串存在性代替真实行为测试。
- 页面模块必须在浏览器验证正常态、空数据、加载中、失败态，并检查控制台无新增错误。
- 工作流模块必须验证刷新恢复、返回再进入、重复点击和连续操作。

### D. 集成验证

- 验证上游输入和下游消费者仍兼容。
- API/数据库变更验证旧记录、缺省字段、重复初始化和错误返回。
- 涉及外部服务（地图、大模型、生图、RAG、数据接口）时，必须把“外部服务直连”“本项目API”“同一服务进程中的相邻核心API”分开验证。目标接口成功不代表系统可用，目标接口失败也不得拖垮无关功能。
- 本地服务的启动方式、网络权限、环境变量或处理器缓存发生变化后，必须重启到最终运行状态，并至少冒烟验证：站点可访问、目标API可调用、AI生成主链路未被连带破坏。不得只用脱离服务器的独立脚本通过来代替系统验证。
- 外部请求必须设置独立异常边界和可操作错误信息；禁止让一次 `fetch` 失败中断同一流程的其他检索通道，禁止把原始 `fetch failed` 直接作为最终用户提示。
- PPT 变更至少验证：材料被读取、大纲关卡、模板缩略图比例、逐页编辑、预览与导出一致、导出前复核。
- 计算变更至少验证：默认基准、边界值、汇总勾稽和导出数值。

### E. 交付

- 列明实际执行的测试和结果。
- 标注未验证条件，不用“应该可以”替代验证。
- 仅在用户明确要求时执行 Git 提交或推送；一个提交只包含一个逻辑变更，不夹带无关文件。

## 4. 风险分级与测试矩阵

### 测试成本与 Token 控制

- 单模块、低耦合修改如果不改变公共接口、共享数据结构、脚本加载顺序或其他模块行为，默认只执行：变更文件语法检查、该模块直接相关测试、受影响页面或功能的真实操作验证。
- 不因修改了一个样式、文案、局部空值保护或单一组件，就机械运行全仓库测试；测试范围必须与实际影响范围相匹配，减少无效运行时间、日志输出和 Token 消耗。
- 优先采用“由小到大”的验证顺序：静态检查 → 单个用例 → 模块测试 → 相邻集成测试 → 全量测试。前一级通过且没有跨模块风险时，可以停止扩大测试范围。
- 只有出现以下任一情况时，才升级到全量回归：修改公共接口或全局状态、改变共享数据结构、调整数据库或鉴权、修改计算公式或导出链路、涉及三个及以上模块、局部测试发现连锁异常、无法准确判断影响边界。
- 外部API的局部容错、错误文案或单一查询策略修改，若接口契约不变且相关API测试、真实连通性和相邻主链路冒烟均通过，可不运行全量测试；若同时改变服务启动方式、网络权限、公共请求封装、环境适配层，或已观察到其他功能连带失败，则必须升级为全量回归。
- 不得为了减少 Token 而跳过必要验证。若未运行全量测试，最终交付必须明确说明采用了哪些局部测试、为什么局部测试足够，以及未覆盖的边界。

### 低风险：文案、局部 CSS、非关键展示

- 语法/静态检查（如适用）。
- 受影响页面浏览器实测和截图核对。
- 相关小范围测试。
- 通常不运行全量测试，避免小改动产生无效等待。

### 中风险：页面状态、工作流、普通 API、模块接线

- 模块测试。
- 真实浏览器端到端操作。
- 涉及的相邻模块测试。
- 若接口契约、共享状态和其他模块均未改变，且局部测试已经覆盖正常态、失败态和相邻调用，可停止在相邻集成测试；只有触发本节“升级到全量回归”的条件时才运行全量测试。

### 外部API故障复盘规则（高德地图事故）

- 事故现象：目标地址足够具体，但服务进程因网络权限导致高德请求 `EACCES/fetch failed`；如果只修改地址匹配或只跑纯函数测试，会把运行环境问题误判成业务问题。
- 事故影响：同一服务进程中的其他API可能同时不可用，因此“地图候选恢复”不能作为“AI可研生成链路正常”的唯一证据。
- 后续固定检查顺序：①确认本地服务进程和端口；②确认外部服务协议级连通；③验证带项目配置的目标API；④验证页面调用目标API；⑤冒烟验证AI生成、RAG/知识检索及至少一个无关核心API；⑥按风险条件决定是否运行全量测试。
- 任何一步失败，必须明确区分：输入不精确、第三方无结果、密钥/额度问题、网络/VPN/防火墙问题、项目API异常、前端调用异常。不得统一归因为“请填写更精确地址”。
- 候选类功能只展示真实返回结果，不伪造数量；应采用多查询合并、去重和人工确认，并允许用户在原位置修改后重试，不能强迫重新开始整个流程。

### 高风险：测算公式、鉴权、数据库迁移、文件导出、公共数据结构

- 修改前后基准快照与边界测试。
- 相关格式的真实导出/解析验证。
- 跨模块端到端验证。
- 全量测试全部通过后才算完成。

风险不确定时按高一级执行。

## 5. 数据、安全与清理

- 密钥只允许来自环境变量，不写入源码、测试、日志、文档或 Git。
- 不代用户输入密码，不读取浏览器 Cookie、LocalStorage 或 SessionStorage 中的敏感数据。
- 不删除、覆盖用户项目和正式资料。测试数据使用 `[系统测试]` 前缀，并在确认目标无误后清理；无法安全清理时明确告知。
- 破坏性操作、数据迁移、批量替换和外部发布必须先确认精确范围。

## 6. 完成定义（Definition of Done）

只有同时满足以下条件，功能才可称为完成：

- [ ] 需求对应的可见行为已经实现。
- [ ] 改动局限在必要模块，没有夹带重构。
- [ ] 模块自检通过。
- [ ] 按风险等级完成集成与回归测试。
- [ ] 浏览器真实路径验证通过（页面功能适用时）。
- [ ] 刷新、返回、重复操作和错误态没有新增问题。
- [ ] 用户数据、旧配置和旧项目保持兼容。
- [ ] 已说明变更文件、测试结果和剩余边界。

遇到基线失败、用户改动冲突、需求会改变数据口径、或必须扩大修改范围时，暂停集成并先说明原因，不通过猜测继续扩大改动。

## 7. MD成果文件默认存放规则

- 方案、总结、实施步骤、分析报告等 Markdown 成果，默认保存到：`C:\Users\HP\Desktop\复刻网站消化\study\AI办公网站`。
- 文件名统一以生成当天的 `MMDD` 日期开头，例如：`0825XX方案.md`。
- 除非用户明确指定其他文件夹，否则无需再次询问输出位置。
- 代码仓库自身必须保留的技术文档、README、迁移说明和模块内文档，仍放在其职责对应的仓库目录，不受本条影响。

## 8. 历史MD归档与合并规则

- 历史方案、阶段总结、调研、实施路线等MD，统一归档到 `C:\Users\HP\Desktop\复刻网站消化\study\AI办公网站` 下的功能板块目录。
- 默认功能板块包括：项目总体与架构、AI可研与财务测算、知识库与协同、AI PPT与视觉、项目数据分析、部署安全与运维、技术学习与代码清单；新增板块时必须同步更新总索引。
- 同一板块存在多份历史资料时，必须生成一份带目录和来源说明的板块合并文档；不能仅把文件堆放到同一文件夹。
- 所有新生成、复制或合并后的MD文件名必须以资料形成日期或归档日期的 `MMDD` 开头。无法可靠判断形成日期时，使用文件最后修改日期；仍无法判断时使用归档当天日期。
- 归档默认采用“保留原件、复制归类、哈希去重、生成索引”的安全方式，不删除或覆盖历史原件。只有用户明确要求清理时才允许移动或删除。
- 项目代码仓库中的README、部署说明和模块技术文档保留在原职责目录；可以复制到外部知识目录做汇总，但不得因归档而破坏仓库文档结构。

## 9. 项目30维持续评估规则

- 每次完成一个实质性开发、架构、数据、知识库、测算、PPT、部署或流程优化任务后，必须进行不少于30个重要维度的项目评估，用于判断当前水平、变化幅度和下一步优先级。
- 固定评估维度至少覆盖：业务覆盖、白箱数值可信度、端到端闭环、人工审签、可解释性、证据追溯、工具注册与发现、参数校验、循环熔断、任务持久化、断点恢复、人工中断审批、异步任务队列、会话隔离、多Agent协同、短期记忆、长期记忆、上下文压缩、RAG检索、Wiki治理、结构化数据溯源、数据Provider、OCR解析、多模型路由、模型故障切换、提示词与版本治理、结构化输出、安全鉴权、细粒度权限、租户隔离、密钥管理、审计日志、可观测性、离线评测、回归测试、并发扩展、部署可移植性、成本控制、SLO告警、交互恢复、降级容错、管理员可维护性、用户反馈学习、真实项目验证。
- 每个维度采用0–100分，必须同时写明：当前证据、主要缺口、下一步动作；不得只给主观分数。
- 若本次变更没有影响某维度，沿用上次分数并标记“无变化”；不得为了体现进展虚增分数。
- 完整评估表默认保存为 `MMDD项目企业级能力30维持续评估.md` 到AI办公网站目录；最终交付消息至少说明综合分、提升项、下降项和最优先的3项行动。
- 低风险文案或局部样式修改可以复用最近一次完整评估基线，仅更新受影响维度和综合分；但每个自然日首次实质任务必须输出完整30维以上基线。

## 10. 三层能力改进罗盘

本节不是要求每次修改都扩大范围，而是用于在开始前识别真实短板、在完成后判断是否有证据提升。任何评分都必须服务业务，不为了追求框架功能数量而增加复杂度。

### 10.1 Agent专项12维

Agent框架、对话、工具、记忆、工作流、MCP或运行时相关任务，至少逐项考虑：

1. 架构设计：业务编排、运行时、工具、记忆、知识、权限和界面是否解耦。
2. Agent循环能力：最大轮次、总耗时、成本预算、重复调用熔断、自查和澄清是否完备。
3. 工具调用系统：注册、发现、Schema、版本、权限、超时、重试、幂等、熔断和审计。
4. 多Agent支持：是否真的存在可并行、上下文可隔离的任务；不得为提高分数盲目拆Agent。
5. 记忆与学习：用户偏好Memory、项目事实State、公司知识Wiki、流程技能Skill是否分层；自动学习是否经过审批、评测和回滚。
6. 知识/RAG集成：检索范围、证据引用、版本效力、来源追溯、低置信过滤和权限隔离。
7. MCP/外部集成：先接统一工具网关，再以适配器方式接MCP；必须有限权、超时、错误隔离、健康检查和审计，不允许MCP绕过工具策略。
8. 可视化与交互：任务进度、工具执行、澄清、审批、失败恢复、返回上一步和结果差异是否清楚。
9. 部署难度：本地/服务器可移植、依赖体检、容器化、备份恢复和离线能力。
10. 代码质量与工程化：模块边界、公共契约、测试、版本兼容、可观测性和小改动准入。
11. 生态成熟度：外部社区热度只作参考；本项目重点看内部插件/Skill/Provider规范、文档、示例、升级兼容和维护责任人。
12. 业务适配度：是否真正服务可研、白箱测算、资料治理、审查签发和版本追溯，数字不得由AI臆造。

Agent专项当前重点关注低项：任务级Checkpoint、多Agent的合理边界、Memory/State/Wiki/Skill分层、Skill候选审批、MCP适配器、上下文安全压缩、结构化输出、细粒度权限、SLO和真实项目评测。

### 10.2 AI PPT专项12维

PPT材料解析、对话流程、大纲、模板、逐页设计、编辑、素材、审查或导出相关任务，至少逐项考虑：

1. 模板体系：模板分类、整套风格一致性、真实模板/生成模板双轨、版本和适用场景。
2. 设计系统：颜色、字体、网格、间距、层级、留白、图形语言和品牌规范。
3. 页面组件库：封面、目录、过渡、卡片、对比、流程、时间轴、图表、表格、图片和结论页覆盖率。
4. 大纲生成质量：材料理解、章节脉络、可编辑层级、子节增删升降级和人工确认关卡。
5. 内容到视觉映射：根据内容类型、数据密度和表达目标选择版式，而不是随机套模板。
6. 数据可视化：表格和数字事实识别、图表选择、测算数据联动、单位口径和来源标注。
7. 部门场景适配：可研、投资汇报、经营分析、工作总结、政策解读等不同场景的专属策略。
8. 人工编辑效率：直接编辑、全局编辑、拖拽/替换、接受拒绝、撤销、批量修改和对话修改。
9. 迭代优化闭环：用户反馈、页面重生成、差异比较、偏好沉淀、模板/组件候选审批。
10. 素材调度：上传图片、Banana生成图、图标、图表和历史优质素材的检索、授权、复用及后台治理。
11. 导出质量：预览与PPT必须消费同一设计数据；字体、比例、图片、表格、可编辑性和兼容性一致。
12. 视觉审查：溢出、遮挡、重复、密度、对齐、对比度、风格一致性和图片级视觉QA。

PPT专项不得只验证“成功导出”；至少验证材料被真实读取、大纲关卡存在、模板缩略图可识别、逐页编辑可用、预览导出一致、自动复核结果可操作。

### 10.3 全项目43维

全项目架构、发布、阶段总结或跨模块任务，使用第9节已经规定的43维完整基线。Agent专项12维和PPT专项12维是专题放大镜，不替代43维企业评估；重叠维度共享证据，不重复虚增分数。

### 10.4 开始前与完成后使用方法

- 开始前：标记本次直接影响的维度、风险等级、预期提升和不在范围内的低分项。
- 实现中：只修改责任模块；低分但不在本次范围的事项只记录，不顺手扩张。
- 完成后：只凭代码、测试、浏览器实测、真实项目或运行数据调整分数；只有方案和文档时分数保持不变。
- 若某指标分数低但对本项目价值有限，例如公共开源社区活跃度或无业务必要的多Agent数量，应说明“不追分”的原因，用内部可维护性和业务效果替代。
- Agent、PPT或全项目评估出现低于60分且直接阻塞本次目标的维度，必须纳入实施方案或明确列为阻塞项；不得在最终交付中省略。

## 11. 全站宠物视频素材一致性规则

- 蓝喵视频以 `assets/blue-kitten-sprite-v1.png` 的浅冰蓝毛色、白色胸口/口鼻/爪部、粉色耳鼻和深蓝眼睛为角色基准；安小居视频以当前正式安小居基础图为基准。未经用户确认不得在不同动作中改变角色配色、年龄感、体型、脸型或材质风格。
- 每一段宠物视频制作和接入前，必须先从同一套正式角色素材选取或生成首尾帧；循环动作优先让首尾帧完全一致，避免循环点跳帧。首尾帧、视频中间帧与网站静态姿态必须保持相同曝光、白平衡、明暗、饱和度和毛发/材质色相。
- 每段生成视频进入项目之前必须自动执行颜色一致性核查：把视频代表帧与角色基准图并排比较；发现偏深、偏灰、偏青、过饱和或白色部位染色时，先离线校色或重新生成，不能直接接入后依赖用户发现。
- 不允许用未经量化的网页运行时滤镜补救整段视频偏色。颜色漂移应优先在生成阶段通过关闭风格化/自动调色和缩短动作时长控制；仍有偏差时，对原始视频做离线逐帧色彩匹配并输出新视频资产，再由网页直接播放。网页只负责抠绿与降级，不承担主观调色。
- 动作视频必须把角色头部和五官作为身份锚点：眼睛形状与间距、鼻口结构、脸型、耳位和头身比在所有采样帧保持一致。即梦当前最短时长为4秒，跑、跳等大动作统一优先采用4秒短循环，头部尽量固定，只驱动身体、四肢和尾巴；中间帧出现变脸、成年化、长鼻或眼距变化时必须重新生成或逐帧修复。
- 绿幕统一使用纯色 `#00FF00`，角色边缘不得残留绿边或被抠色污染。抠绿后还要复核耳尖、毛发、尾尖、手脚等细边缘，并确认颜色校正没有损伤透明边缘。
- 视频验收至少覆盖：首帧、25%、50%、75%、尾帧五个采样点；检查角色身份、色调、清晰度、肢体结构、镜头稳定、动作连续和首尾循环。任一采样点明显变色、变形或漂移时，该视频不得标记为可接入。
- 页面接入时保留静态图片降级；视频加载失败、减少动画、页面隐藏或性能不足时退回同色调静态角色，不允许出现另一套颜色或角色形象。
- 对仅持续少量帧的头顶小点、局部增生、闪点等小范围瑕疵，优先使用相邻干净帧进行局部ROI逐帧修复并羽化边缘，保留原片、音频、时长和未修复版本；修复后重新执行五点采样和完整解码检查。只有脸型、主体轮廓或大面积肢体结构失真时才要求重新生成，避免无谓消耗生成费用。
- 相邻视频默认禁止旧帧与新帧交叉混合，避免生成双猫、重影或半透明肢体；应保留上一段末帧，待下一段首帧就绪后做动作匹配切换。网页位移必须通过逐帧采样绑定到真实迈步、离地等动作时点，不能从站立或蓄力首帧开始整体平移；跳跃类位移还必须在落地站稳前结束，禁止落地后继续滑动。连续同名动作的延迟任务必须用动作级令牌隔离。
- 此后每一条宠物动作视频提示词必须包含“严格只改变既定动作，不得额外新增、删除或改变任何内容；禁止睫毛、头顶圆点、凸起、天线、饰品及新结构”，并以“每一帧动作都要连贯。”作为固定结尾，明确约束生成过程不得出现角色增生、断帧或跳帧。

### 11.1 动作链与拐角转场准入

- 验收对象不只是单个视频，还必须覆盖“前一片尾帧 → 后一片首帧”。每次新增或替换视频后，按真实随机清单和长方形路线生成相邻片段对照表；姿态、朝向、角色大小、中心位置、曝光任一不一致，都不得直接接入。
- 姿态名称必须描述真实画面，不能把正面跑姿、背面跑姿或腾空姿态笼统标成 `H1`。蓝喵至少区分 `H0`坐姿、`H1_LEFT/H1_RIGHT`侧面站姿、`RUN_FRONT_A/B`、`RUN_BACK_A/B`、`AIR`；安小居建立自己的同类状态，不能复用蓝喵姿态名掩盖差异。
- 四边跑动必须同时验收两种路线方向的8个拐角连接：右→下、下→左、左→上、上→右，以及左→下、下→右、右→上、上→左。侧面站姿与正/背面步态之间必须使用真实入场/收尾视频，禁止静态镜像、瞬间翻转、CSS旋转、整体滑动或交叉淡化冒充转身。
- 方向动作采用“入场原地转向 → 主跑片段负责网页位移 → 收尾原地恢复标准站姿”的三段式。位移开始点绑定主跑首个真实迈步帧，位移结束点早于收尾片段；视频提前结束时仍须等待网页位移完成，不能提前移除 `pet-moving` 导致瞬移。
- 每个相邻片段至少抽取前片尾部5帧与后片头部5帧，检查跳帧、亮帧、重影和身份变化；随后在正式页面按100～200毫秒采样按钮坐标，确认单轴连续移动、拐角无对角穿越、动作结束无坐标补跳。

### 11.2 新角色与单视频替换工作流

- 安小居接入前先确定独立角色基准图、`H0/H1`及各方向步态锚点，再制作动作链；不得直接套用蓝喵视频、颜色统计或脸部锚点。蓝喵与安小居使用角色隔离的素材目录和清单，但共用播放器、调度、位移同步、失败降级和验收工具。
- 新增动作只增加独立 clip 与动作映射；替换旧动作只修改该 clip 的文件、版本、裁剪和姿态契约，禁止要求其他视频重新生成。原片按角色保留在 `outputs/pet-video-source/蓝猫/`、`outputs/pet-video-source/安小居/`，网页正式片放在 `assets/pets/videos/<角色>/` 或兼容目录。
- 固定执行顺序：基准角色确认 → 动作链拆分 → 中文命名首尾帧 → 200字内提示词 → 生成视频 → 五点单片检查 → 十帧相邻接缝检查 → 离线颜色/绿边处理 → 清单注册 → 定向测试 → 正式页面随机至少5轮。任一步失败只返工对应单片。
- 宠物问题与通用解法维护在 `assets/pets/videos/README.md`；角色提示词、素材映射和验收结果分别维护在 `outputs/pet-video-source/蓝猫/宠物视频提示词汇总.md`、`outputs/pet-video-source/安小居/安小居视频提示词汇总.md`。本文件保留不可绕过的工程准入规则。
