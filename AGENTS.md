# 项目协作与变更准入规则

> 2026-09-10存储验收补充：数据库依赖测试跳过不等于通过。发布前运行npm run test:integration，使用脚本创建并清理的独立PostgreSQL测试库，核对0失败、0跳过。性能必须注明样本和口径；序列化缓存量不等于物理占用，保存耗时改善不等于整站提速。未同步/冲突副本不得为满足预算自动删除。

本文件是本仓库所有代码修改的默认工程约束。目标是：改动尽可能小、模块先自测再集成、问题可定位、结果可回退。除非用户明确要求，否则只在本地修改，不提交或推送 Git。

> 边界说明：本文件只存放项目协作与变更规范。Agent 原理、Hermes 学习、核心代码解读和升级路线统一维护在 `outputs/0826Agent学习总成.md`，不得把教程内容混入本规范。

## 1. 基本原则

1. 先确认现状，再修改。开始前检查 `git status --short`，用户已有改动不得覆盖、清理或顺手重构。
2. 一次只解决一个清晰问题。优先修改责任模块，不跨模块复制逻辑，不借机改名或格式化无关文件。
3. 优先最小补丁。能修一处入口就不同时包装多个入口；能补兼容层就不破坏旧数据。
4. 模块必须先通过自身检查，才允许接入公共入口、导航、数据库或导出链路。
5. 测试强度按风险选择，小改动不机械运行全部测试；但计算、导出、鉴权、数据库和跨模块工作流必须做完整回归。
6. 最终交付必须说明：改了什么、涉及哪些文件、做了哪些验证、仍有哪些边界；不得把“代码已写”当成“功能完成”。

## 2. 主要模块边界

- 页面导航与登录恢复：`app.js`、`auth.js`、`ui-route-state.js`。
- AI PPT 对话与流程：`ppt-conversation-*.js`、`ppt-agent-workflow.js`、`ppt-guided-flow-v2.js`。
- PPT 页面设计、预览与导出：`ppt-components.js`、`ppt-design-ir.js`、`ppt-export*.js`。预览与导出必须消费同一份设计数据，不得维护两套视觉逻辑。
- PPT 素材中心：`ppt-asset-center.*`、`ppt-asset-admin.js`、`functions/api/pptassets.js`。
- 可研生成与复核：`report.js`、`review.js`、相关 `functions/api/`。
- 财务测算：`nrcalc.js`、`rentcalc.js`、`salecalc.js`、`investestimate.js`。公式修改必须有数值回归测试。
- 本地服务与存储：`local-server/`、`migrations/`。数据库变更必须向后兼容并可重复执行。

若一个需求同时影响多个模块，先写清输入、输出和责任模块，再按“底层模块 → 适配层 → 页面入口”的顺序集成。

## 3. 每次变更的准入流程

### A. 基线核查

- 检查工作区状态，识别用户未提交改动。
- 找到功能唯一入口、数据来源和现有测试。
- 记录修改前可复现现象；若基线本身失败，先报告并区分旧问题与本次问题。

### B. 模块内实现

- 只修改必要文件；新增能力优先放在职责明确的独立模块。
- 公共接口保持最小，只暴露调用方需要的方法和数据。
- 不允许隐式修改其他模块的全局状态。
- 本项目大量脚本由普通 `<script>` 加载，共享全局作用域：新增常量、函数必须使用模块前缀或 IIFE，避免同名声明导致整文件失效；`index.html` 的脚本顺序视为接口契约。

### C. 模块自检

模块未通过以下检查，不得汇入主流程：

- JavaScript 语法检查：`node --check <变更文件>`。
- 对应单元/回归测试；优先验证行为和输出，不以字符串存在性代替真实行为测试。
- 页面模块必须在浏览器验证正常态、空数据、加载中、失败态，并检查控制台无新增错误。
- 工作流模块必须验证刷新恢复、返回再进入、重复点击和连续操作。

### D. 集成验证

- 验证上游输入和下游消费者仍兼容。
- API/数据库变更验证旧记录、缺省字段、重复初始化和错误返回。
- 涉及外部服务（地图、大模型、生图、RAG、数据接口）时，必须把“外部服务直连”“本项目API”“同一服务进程中的相邻核心API”分开验证。目标接口成功不代表系统可用，目标接口失败也不得拖垮无关功能。
- 本地服务的启动方式、网络权限、环境变量或处理器缓存发生变化后，必须重启到最终运行状态，并至少冒烟验证：站点可访问、目标API可调用、AI生成主链路未被连带破坏。不得只用脱离服务器的独立脚本通过来代替系统验证。
- 外部请求必须设置独立异常边界和可操作错误信息；禁止让一次 `fetch` 失败中断同一流程的其他检索通道，禁止把原始 `fetch failed` 直接作为最终用户提示。
- PPT 变更至少验证：材料被读取、大纲关卡、模板缩略图比例、逐页编辑、预览与导出一致、导出前复核。
- 计算变更至少验证：默认基准、边界值、汇总勾稽和导出数值。

### E. 交付

- 列明实际执行的测试和结果。
- 标注未验证条件，不用“应该可以”替代验证。
- 每次完成实质性代码更新，最终交付必须运行 `node scripts/count-source-code.mjs`，报告当前有效源码与结构化配置的文件数、精确总字节数、MiB 和总行数；不得只写“代码很多”或沿用历史数字。统计口径固定为 JS/MJS/CJS/HTML/CSS/SQL/Python/JSON，并排除 `.git`、`node_modules`、`outputs`、`.dsh-filess`、`.tmp`、`local-data` 和 `tools/vendor`。纯问答且没有代码变化时可注明“本次未改代码，沿用上次统计”。
- 仅在用户明确要求时执行 Git 提交或推送；一个提交只包含一个逻辑变更，不夹带无关文件。

## 4. 风险分级与测试矩阵

### 测试成本与 Token 控制

- 单模块、低耦合修改如果不改变公共接口、共享数据结构、脚本加载顺序或其他模块行为，默认只执行：变更文件语法检查、该模块直接相关测试、受影响页面或功能的真实操作验证。
- 不因修改了一个样式、文案、局部空值保护或单一组件，就机械运行全仓库测试；测试范围必须与实际影响范围相匹配，减少无效运行时间、日志输出和 Token 消耗。
- 优先采用“由小到大”的验证顺序：静态检查 → 单个用例 → 模块测试 → 相邻集成测试 → 全量测试。前一级通过且没有跨模块风险时，可以停止扩大测试范围。
- 只有出现以下任一情况时，才升级到全量回归：修改公共接口或全局状态、改变共享数据结构、调整数据库或鉴权、修改计算公式或导出链路、涉及三个及以上模块、局部测试发现连锁异常、无法准确判断影响边界。
- 外部API的局部容错、错误文案或单一查询策略修改，若接口契约不变且相关API测试、真实连通性和相邻主链路冒烟均通过，可不运行全量测试；若同时改变服务启动方式、网络权限、公共请求封装、环境适配层，或已观察到其他功能连带失败，则必须升级为全量回归。
- 不得为了减少 Token 而跳过必要验证。若未运行全量测试，最终交付必须明确说明采用了哪些局部测试、为什么局部测试足够，以及未覆盖的边界。

### 低风险：文案、局部 CSS、非关键展示

- 语法/静态检查（如适用）。
- 受影响页面浏览器实测和截图核对。
- 相关小范围测试。
- 通常不运行全量测试，避免小改动产生无效等待。

### 中风险：页面状态、工作流、普通 API、模块接线

- 模块测试。
- 真实浏览器端到端操作。
- 涉及的相邻模块测试。
- 若接口契约、共享状态和其他模块均未改变，且局部测试已经覆盖正常态、失败态和相邻调用，可停止在相邻集成测试；只有触发本节“升级到全量回归”的条件时才运行全量测试。

### 外部API故障复盘规则（高德地图事故）

- 2026-09-09 研究正文接收复盘：私人研究的 `currentProjectId=null` 被请求层转成空字符串，返回后与原null比较，误报“已切换项目”。研究任务必须按研究/轮次令牌接收，正式项目按项目ID接收；请求参数归一化不等于任务身份。异步权限复核之后仍须再次校验身份。测试必须包含无正式项目ID的私人研究成功、真实切换项目/轮次被拒绝；不得以删除防串写校验来修复误报。

- 2026-09-09 表格模板接口复盘：PostgreSQL 中文错误信息使英文 `already exists` 判断失效，已有字段被当作初始化失败并返回500。重复字段必须优先识别 SQLSTATE `42701`，D1文本判断仅作兼容；权限、连接等其他错误必须继续上报。涉及此类初始化的修改必须覆盖中文错误、重复读取、已发布配置不变和非重复错误不被吞掉，不能仅用永不抛错的数据库 mock 验收。接口500与正文生成失败应分别验证，不得未经证据归为同一根因。

- 事故现象：目标地址足够具体，但服务进程因网络权限导致高德请求 `EACCES/fetch failed`；如果只修改地址匹配或只跑纯函数测试，会把运行环境问题误判成业务问题。
- 事故影响：同一服务进程中的其他API可能同时不可用，因此“地图候选恢复”不能作为“AI可研生成链路正常”的唯一证据。
- 后续固定检查顺序：①确认本地服务进程和端口；②确认外部服务协议级连通；③验证带项目配置的目标API；④验证页面调用目标API；⑤冒烟验证AI生成、RAG/知识检索及至少一个无关核心API；⑥按风险条件决定是否运行全量测试。
- 任何一步失败，必须明确区分：输入不精确、第三方无结果、密钥/额度问题、网络/VPN/防火墙问题、项目API异常、前端调用异常。不得统一归因为“请填写更精确地址”。
- Codex启动或重启`local-server/server.js`时必须使用正常网络权限；禁止从限制外网的沙箱进程直接派生正式服务。重启后必须看到“默认AI Provider网络可达”并完成一次真实`/api/generate`请求，才能把服务交还用户。
- 候选类功能只展示真实返回结果，不伪造数量；应采用多查询合并、去重和人工确认，并允许用户在原位置修改后重试，不能强迫重新开始整个流程。

### 联网检索假成功复盘（2026-09-09）

- 2026-09-10 草稿缓存配额复盘：修复恢复窗口不等于修复生成前保存入口。所有冲突备份入口均须覆盖缓存满场景；完整原副本仍在原键时可保护原键并继续服务器版本校验保存，不得再覆盖原键。无法确认原副本完整或服务器保存失败必须阻止生成；不得通过清空用户缓存或移除版本校验解决。必须验证重复保存、服务器失败及其他页面更改原键。

- 2026-09-10 可研打开/关闭准入：关闭覆盖在报告上的项目面板，不应等待未离开的整篇报告保存；真正切换项目/研究仍必须保存并校验身份。只需身份和权限时使用轻量身份接口，不复制整份研究正文。恢复页面避免重复绘制，卡片初始展开事件不得触发保存。研究HTTP请求与存储体积上限须一致兼容历史长报告，超过上限明确报错并保留旧稿，禁止截断内容。必须实测长报告打开、关闭、重新进入和刷新；自动测试通过或URL已变化不能代替页面可操作验收，浏览器连接超时须标注未验证。

- HTTP 200、端口可达或一次普通AI对话成功，均不能证明联网检索可用。必须核验搜索响应的完成状态、真实工具调用/引用及非空网页候选；`incomplete/max_output_tokens`、未执行工具、DSML文本或模型口述“正在搜索”不得视为成功，也不得将推测网址沉淀为证据。
- 主搜索失败时保持独立异常边界，验证至少一个已启用备用通道的真实结果；无通道可用须明确提示连接/超时/配置/协议错误，不能只显示“暂无结果”或“全部检索完成”。真实无匹配与请求失败必须分开呈现。
- 批量检索必须保留各任务错误并允许只重试失败项，不重跑成功项、不自动采用候选、不放松质量门槛。旧缓存失败任务也必须展示错误。
- 网络诊断脚本遇到 EACCES 必须在正常网络权限下复验，不能把沙箱诊断失败当成正式服务故障。启动自检后的真实验收至少含：搜索API返回真实候选、生成API返回正文、相邻核心API正常；不能用模型401连通探针替代这些验收。
- 报告“失败1”必须取得具体小节错误并区分模型请求、任务轮询、前端处理、权限/版本隔离。不得仅凭检索失败推断正文同因，不得为修复而绕过隔离或重写已成功章节。

### 高风险：测算公式、鉴权、数据库迁移、文件导出、公共数据结构

- 修改前后基准快照与边界测试。
- 相关格式的真实导出/解析验证。
- 跨模块端到端验证。
- 全量测试全部通过后才算完成。

风险不确定时按高一级执行。

## 5. 数据、安全与清理

### 2026-09-10 浏览器存储治理准入

- 不以扩大localStorage或把整份JSON原样搬入IndexedDB作为性能完成证据。编辑合并后才生成快照，内容分块去重，工具栏/权限探针只取身份摘要。
- 旧缓存删除前必须独立备份、完整读回校验，并复核账号/项目/轮次和原值未变；未同步、冲突、唯一原件不参与普通缓存淘汰。
- 本机存储超时须中止事务，晚到回执再次核对身份；本机失败不能阻止远端正常保存。回执只确认对应版本，不确认后续输入。
- 新manifest发布先部署双读后端；RESEARCH_STORAGE_V2=0仅关闭分块写入，不允许直接回滚到无法读取新格式的旧程序。
- 至少验证真实数据库内容损坏拒绝、版本冲突、响应丢失重放，以及隔离浏览器刷新、配额保护、旧格式兼容。容量承诺必须有实际混合负载，不以单次成功代替。
- 2026-09-10容量口径：分块可研的正文、候选和历史逻辑总量为128MiB，单轮次去重资料块为256MiB；正常上传批1MiB，单批硬上限2MiB，保存清单为20MiB。20MiB是单次网络包保护线，不是项目总容量；不得为“支持100MB”把整份JSON放宽到100MB。

- 密钥只允许来自环境变量，不写入源码、测试、日志、文档或 Git。
- 不代用户输入密码，不读取浏览器 Cookie、LocalStorage 或 SessionStorage 中的敏感数据。
- 不删除、覆盖用户项目和正式资料。测试数据使用 `[系统测试]` 前缀，并在确认目标无误后清理；无法安全清理时明确告知。
- 破坏性操作、数据迁移、批量替换和外部发布必须先确认精确范围。

## 6. 完成定义（Definition of Done）

### 可研正文与Word通用成稿门禁（0909）

- 2026-09-10用户调整：120页保留为精简目标；实际排版已确认超页时，用户可明确确认“仍然下载”完整报告，此项覆盖下文超页禁止下载规则。排版失败或无有效页数仍拦截，不截断、不缩字，不把超页报告标为达标。

- 通用成稿内容规则更新必须进入候选稿影响范围，不能只依赖小节的 staleKind=logic。以内容规则版本记录确认状态，接受候选稿后写入、撤销时恢复；锁定和已有候选稿不得覆盖。待补颜色属于渲染规则，不单独消耗AI重生成。恢复保存冲突前必须比较并备份两份快照，再按服务器版本校验保存；大报告备份不得仅依赖容量有限的localStorage。浏览器验收失败不得宣称冲突已解除。

- 所有项目的依据类章节（含编制、政策、法规、项目专属依据及下级小节）只列依据文件清单及已核实的文号、发布机关、日期，不展开解读、适用性分析、项目对照、核对过程或小结；未知信息不编造。此规则优先于旧依据章节的深度和字数要求。政策文件出处属于清单必要信息，不得因删除正文“来源：”说明而一并删除。旧正文不通过正则臆断删改，须重新生成对应依据小节或人工确认。

- 普通正文黑色，但完整的“【待补：…】”必须红色；不得用全局黑字覆盖缺失提示。正文、普通表、标准模板表均应验证真实Word颜色。
- 明确的TABLE标记即使缺失闭合符，也要恢复表结构；注释不得吞进表内。不推测或填造缺失数值，无法确定的结构须提示补充。
- 默认不自动附加财务附表、图片附件、来源清单或冻结引用快照；原件、计算结果和后台溯源记录保留，不因简版导出删除。
- 正文不展示“来源：/资料来源：/数据来源：”说明或括注，但不得删除“收入来源”等业务事实。规则集中维护在report-output-policy.js。
- 全篇（不含附件）最多120页，生成应精简而非凑满上限。下载前调用实际排版核验，超过120页或排版服务失败不得误报通过，不截断正文、不缩字规避。当前Windows依赖WPS，Linux依赖LibreOffice和pdfinfo，纯静态部署需配置同等排版服务才能启用该下载门禁。
- 页数按服务器排版引擎核验；不同终端字体和Word/WPS版本仍可能重排，交付须说明这一边界。小样导出成功不能替代真实长报告及超限、失败场景验证。

只有同时满足以下条件，功能才可称为完成：

- [ ] 需求对应的可见行为已经实现。
- [ ] 改动局限在必要模块，没有夹带重构。
- [ ] 模块自检通过。
- [ ] 按风险等级完成集成与回归测试。
- [ ] 浏览器真实路径验证通过（页面功能适用时）。
- [ ] 刷新、返回、重复操作和错误态没有新增问题。
- [ ] 用户数据、旧配置和旧项目保持兼容。
- [ ] 已说明变更文件、测试结果和剩余边界。

遇到基线失败、用户改动冲突、需求会改变数据口径、或必须扩大修改范围时，暂停集成并先说明原因，不通过猜测继续扩大改动。

## 7. MD成果文件默认存放规则

- 方案、总结、实施步骤、分析报告等 Markdown 成果，默认保存到：`C:\Users\HP\Desktop\复刻网站消化\study\AI办公网站`。
- 文件名统一以生成当天的 `MMDD` 日期开头，例如：`0825XX方案.md`。
- 除非用户明确指定其他文件夹，否则无需再次询问输出位置。
- 代码仓库自身必须保留的技术文档、README、迁移说明和模块内文档，仍放在其职责对应的仓库目录，不受本条影响。

## 8. 历史MD归档与合并规则

- 历史方案、阶段总结、调研、实施路线等MD，统一归档到 `C:\Users\HP\Desktop\复刻网站消化\study\AI办公网站` 下的功能板块目录。
- 默认功能板块包括：项目总体与架构、AI可研与财务测算、知识库与协同、AI PPT与视觉、项目数据分析、部署安全与运维、技术学习与代码清单；新增板块时必须同步更新总索引。
- 同一板块存在多份历史资料时，必须生成一份带目录和来源说明的板块合并文档；不能仅把文件堆放到同一文件夹。
- 所有新生成、复制或合并后的MD文件名必须以资料形成日期或归档日期的 `MMDD` 开头。无法可靠判断形成日期时，使用文件最后修改日期；仍无法判断时使用归档当天日期。
- 归档默认采用“保留原件、复制归类、哈希去重、生成索引”的安全方式，不删除或覆盖历史原件。只有用户明确要求清理时才允许移动或删除。
- 项目代码仓库中的README、部署说明和模块技术文档保留在原职责目录；可以复制到外部知识目录做汇总，但不得因归档而破坏仓库文档结构。

## 9. 项目44维持续评估与60维战略复核规则

- 每次完成一个实质性开发、架构、数据、知识库、测算、PPT、部署或流程优化任务后，必须使用44个核心维度评估当前水平、变化幅度和下一步优先级；架构、发布、季度复盘或跨模块任务再使用60维战略复核，普通局部修改不得为凑维度扩大范围。
- 固定评估维度至少覆盖：业务覆盖、白箱数值可信度、端到端闭环、人工审签、可解释性、证据追溯、工具注册与发现、参数校验、循环熔断、任务持久化、断点恢复、人工中断审批、异步任务队列、会话隔离、多Agent协同、短期记忆、长期记忆、上下文压缩、RAG检索、Wiki治理、结构化数据溯源、数据Provider、OCR解析、多模型路由、模型故障切换、提示词与版本治理、结构化输出、安全鉴权、细粒度权限、租户隔离、密钥管理、审计日志、可观测性、离线评测、回归测试、并发扩展、部署可移植性、成本控制、SLO告警、交互恢复、降级容错、管理员可维护性、用户反馈学习、真实项目验证。
- 每个维度采用0–100分，必须同时写明：当前证据、主要缺口、下一步动作；不得只给主观分数。
- 若本次变更没有影响某维度，沿用上次分数并标记“无变化”；不得为了体现进展虚增分数。
- 统一证据等级：0分=不存在；20分=只有设想或文档；40分=隔离原型；60分=进入主流程但闭环不足；75分=有自动测试和版本治理；85分=具备权限、审计、失败恢复；95分=多个真实项目和生产指标持续验证；100分=长期规模化运行且有独立复核。中间分必须能落到相邻等级的具体证据，不能凭印象给分。
- 统一评分上限：未进入真实主流程最高59；无行为测试最高69；无版本/权限/审计最高84；无真实项目最高89；无多人、多项目、跨版本和持续运行证据最高94。方案或文档本身不得直接提高实现分。
- 必须分开报告“能力覆盖平均分”“生产成熟度加权分”“真实数据资产成熟度”，不得用功能丰富度单一高分代替企业生产能力。维度重叠时共享证据、只计一次；不适用项标记N/A并说明原因，不以满分代替。
- 证据必须写明代码/测试/运行记录/真实项目的日期和适用版本；超过一个大版本或已被架构替换的证据标记过期，不能继续支撑当前高分。
- 60维战略复核在44个核心维度外增加16项：45产品定位与边界、46垂直业务与AI可研专业性、47可研规则及数据需求/寻源治理、48 Word成果质量、49专业表格治理、50 Project Context与Project Brain、51 Task Graph/幂等/依赖失效、52规则范围/发布/回滚、53 Golden Project与Training/Holdout、54生命周期与Investment Ops、55文件智能/统一搜索/大文件对象存储、56 OA/组织/多用户协作准备、57 CI/CD与Release Gate、58单一事实源与Release Consistency、59数据库Migration/灾备/高可用、60性能容量与运营监控。
- 完整评估表默认保存为 `MMDD项目企业级能力44维持续评估.md` 到AI办公网站目录；60维战略复核文件名使用 `MMDD项目企业级能力60维战略复核.md`。最终交付消息至少说明三类综合分、提升项、下降项和最优先的3项行动。
- 低风险文案或局部样式修改可以复用最近一次完整评估基线，仅更新受影响维度和综合分；但每个自然日首次实质任务必须输出完整44维基线。60维战略复核不要求每轮重复，只有触发上述架构、发布、季度或跨模块条件时执行。

## 10. 三层能力改进罗盘

本节不是要求每次修改都扩大范围，而是用于在开始前识别真实短板、在完成后判断是否有证据提升。任何评分都必须服务业务，不为了追求框架功能数量而增加复杂度。

### 10.1 Agent专项12维

Agent框架、对话、工具、记忆、工作流、MCP或运行时相关任务，至少逐项考虑：

1. 架构设计：业务编排、运行时、工具、记忆、知识、权限和界面是否解耦。
2. Agent循环能力：最大轮次、总耗时、成本预算、重复调用熔断、自查和澄清是否完备。
3. 工具调用系统：注册、发现、Schema、版本、权限、超时、重试、幂等、熔断和审计。
4. 多Agent支持：是否真的存在可并行、上下文可隔离的任务；不得为提高分数盲目拆Agent。
5. 记忆与学习：用户偏好Memory、项目事实State、公司知识Wiki、流程技能Skill是否分层；自动学习是否经过审批、评测和回滚。
6. 知识/RAG集成：检索范围、证据引用、版本效力、来源追溯、低置信过滤和权限隔离。
7. MCP/外部集成：先接统一工具网关，再以适配器方式接MCP；必须有限权、超时、错误隔离、健康检查和审计，不允许MCP绕过工具策略。
8. 可视化与交互：任务进度、工具执行、澄清、审批、失败恢复、返回上一步和结果差异是否清楚。
9. 部署难度：本地/服务器可移植、依赖体检、容器化、备份恢复和离线能力。
10. 代码质量与工程化：模块边界、公共契约、测试、版本兼容、可观测性和小改动准入。
11. 生态成熟度：外部社区热度只作参考；本项目重点看内部插件/Skill/Provider规范、文档、示例、升级兼容和维护责任人。
12. 业务适配度：是否真正服务可研、白箱测算、资料治理、审查签发和版本追溯，数字不得由AI臆造。

Agent专项当前重点关注低项：任务级Checkpoint、多Agent的合理边界、Memory/State/Wiki/Skill分层、Skill候选审批、MCP适配器、上下文安全压缩、结构化输出、细粒度权限、SLO和真实项目评测。

### 10.2 AI PPT专项12维

PPT材料解析、对话流程、大纲、模板、逐页设计、编辑、素材、审查或导出相关任务，至少逐项考虑：

1. 模板体系：模板分类、整套风格一致性、真实模板/生成模板双轨、版本和适用场景。
2. 设计系统：颜色、字体、网格、间距、层级、留白、图形语言和品牌规范。
3. 页面组件库：封面、目录、过渡、卡片、对比、流程、时间轴、图表、表格、图片和结论页覆盖率。
4. 大纲生成质量：材料理解、章节脉络、可编辑层级、子节增删升降级和人工确认关卡。
5. 内容到视觉映射：根据内容类型、数据密度和表达目标选择版式，而不是随机套模板。
6. 数据可视化：表格和数字事实识别、图表选择、测算数据联动、单位口径和来源标注。
7. 部门场景适配：可研、投资汇报、经营分析、工作总结、政策解读等不同场景的专属策略。
8. 人工编辑效率：直接编辑、全局编辑、拖拽/替换、接受拒绝、撤销、批量修改和对话修改。
9. 迭代优化闭环：用户反馈、页面重生成、差异比较、偏好沉淀、模板/组件候选审批。
10. 素材调度：上传图片、Banana生成图、图标、图表和历史优质素材的检索、授权、复用及后台治理。
11. 导出质量：预览与PPT必须消费同一设计数据；字体、比例、图片、表格、可编辑性和兼容性一致。
12. 视觉审查：溢出、遮挡、重复、密度、对齐、对比度、风格一致性和图片级视觉QA。

PPT专项不得只验证“成功导出”；至少验证材料被真实读取、大纲关卡存在、模板缩略图可识别、逐页编辑可用、预览导出一致、自动复核结果可操作。

### 10.3 全项目44维与战略60维

普通实质变更使用第9节规定的44维核心基线；全项目架构、发布、阶段总结或跨模块任务使用60维战略复核。Agent专项12维和PPT专项12维是专题放大镜，不替代全项目评估；重叠维度共享证据，不重复虚增分数。

### 10.4 开始前与完成后使用方法

- 开始前：标记本次直接影响的维度、风险等级、预期提升和不在范围内的低分项。
- 实现中：只修改责任模块；低分但不在本次范围的事项只记录，不顺手扩张。
- 完成后：只凭代码、测试、浏览器实测、真实项目或运行数据调整分数；只有方案和文档时分数保持不变。
- 若某指标分数低但对本项目价值有限，例如公共开源社区活跃度或无业务必要的多Agent数量，应说明“不追分”的原因，用内部可维护性和业务效果替代。
- Agent、PPT或全项目评估出现低于60分且直接阻塞本次目标的维度，必须纳入实施方案或明确列为阻塞项；不得在最终交付中省略。

## 11. 全站宠物视频素材一致性规则

### 正式服务器部署前容量门禁（2026-09-10）

- 用户开始正式服务器部署、发布验收或生产切换时，Agent 必须主动提醒并逐项落实以下三项，不得把本机短时压测通过当作生产容量认证：
  1. 报告章节改为从服务器分段读取，核验首次下载体积、首屏耗时、滚动加载、断网重试及分段保存，不允许首次进入仍下载整份超大报告。
  2. 重任务队列改为可持久化、服务重启后可恢复的队列，核验任务幂等、进度恢复、失败重试、并发上限、用户/项目隔离及审计记录。
  3. 在最终4核8GB服务器上执行至少60分钟持续混合压测，并在压力期间或压测后执行服务重启恢复测试；混合流量必须覆盖浏览、编辑、保存、上传、AI重任务和真实排版导出。
- 正式部署容量结论必须报告请求总量、成功率、错误分类、p50/p95/p99、CPU/内存/磁盘/数据库占用、队列等待时间和重启前后未完成任务恢复率；任何跳过项都不得写成通过。
- 2026-09-10本机4核8GB、300并发、10分钟结果仅作为应用层阶段准入证据，不替代最终服务器、真实浏览器、真实外部AI和60分钟稳定性验收。

### 全项目300人压测与按钮批量回归规则（2026-09-10）

- 用户在任何后续对话中提出“压测、容量、按钮检测、全站回归、发布验收、改完后都点一遍”等要求时，必须主动读取并执行本节，不得只测试AI可研页面，也不得把单元测试数量冒充页面按钮覆盖数。
- 300人阶段压测固定以同一套4核8GB环境、同一版本压测程序和同一业务数据规模做前后对比；至少覆盖浏览、编辑准备、保存、上传和导出，报告300个独立账号、请求总量、成功/失败数、p50/p95/p99、CPU、内存、Node进程、数据库及队列指标。短时合成HTTP结果只证明应用层阶段承压，不代表300台真实浏览器或生产认证。
- 全站按钮回归必须先生成按钮台账，至少分为：登录与全局导航、AI对话、AI可研、个人知识库、投资管理/正式项目、财务测算、材料/OCR、报告复核与导出、AI PPT、后台管理。台账按“页面/弹窗—按钮名称—唯一定位—风险类型—实际重复次数—结果—问题编号”记录；隐藏、禁用、权限限定和条件出现的按钮必须单列，不能从总数中静默遗漏。
- 每个安全且可恢复的按钮必须在真实浏览器中重复5—10次，并覆盖首次点击、连续点击、关闭再开、返回再入、刷新后再点；已经在同一前端版本、同一处理器和同一状态条件下验收过的按钮可引用既有记录，不重复消耗时间，但入口、处理器或状态逻辑变化后必须重测。
- 删除、废止、覆盖、重新开始、退出登录、付费AI、真实上传、真实下载等有副作用按钮，不得在正式数据上机械执行5—10次。必须使用`[系统测试]`数据验证完整确认链；没有隔离测试数据时，只重复验证打开、校验、取消和关闭，并以自动化行为测试覆盖最终提交路径，报告中明确标记“未在正式数据执行”，不得写成完整通过。
- 每轮最终报告必须量化为：全项目识别按钮总数、纳入实测数、按类别按钮数、每类重复次数/总点击次数、跳过或条件受限数及原因、发现问题数、已修复数、未修复数、自动测试通过/失败/跳过数。只有台账、真实页面证据和相关自动回归一致，才能称为本轮按钮批量检测完成。
- 2026-09-10全站基线：按“相同处理器/相同功能去重、重复列表行只算一类、输入框/复选框/键盘快捷键不计按钮”的口径，共识别并纳入204类功能按钮，分为登录与全局入口3、AI对话/办公9、AI可研47、个人知识库18、投资管理/正式项目37、财务测算14、项目数据分析9、报告复核与导出10、AI PPT 30、后台管理27；材料/OCR为上述类别中的5类跨模块子集，不重复计入总数。统一可比验收量为204×5=1020次按钮操作，额外的第6—10次、刷新、键盘及状态准备不并入该总数。该轮合计发现并修复3项：独立AI可研复核入口误要求正式项目、AI PPT粘贴材料后分析按钮未启用、AI PPT增删页面后恢复到错误阶段；复核后未发现未解决按钮阻塞。AI可研47类已有同版本470次既有记录，后续全站回归可引用，不得无变化重复执行。
- 上述基线的详细台账、风险边界和脚本证据存放于`outputs/0910全站按钮批量回归台账.md`。以后入口、事件处理器、权限、保存或恢复逻辑变化时，只重测受影响类别；全站发布验收再重新盘点总数，禁止机械沿用204。

- 蓝喵视频以 `assets/blue-kitten-sprite-v1.png` 的浅冰蓝毛色、白色胸口/口鼻/爪部、粉色耳鼻和深蓝眼睛为角色基准；安小居视频以当前正式安小居基础图为基准。未经用户确认不得在不同动作中改变角色配色、年龄感、体型、脸型或材质风格。
- 每一段宠物视频制作和接入前，必须先从同一套正式角色素材选取或生成首尾帧；循环动作优先让首尾帧完全一致，避免循环点跳帧。首尾帧、视频中间帧与网站静态姿态必须保持相同曝光、白平衡、明暗、饱和度和毛发/材质色相。
- 每段生成视频进入项目之前必须自动执行颜色一致性核查：把视频代表帧与角色基准图并排比较；发现偏深、偏灰、偏青、过饱和或白色部位染色时，先离线校色或重新生成，不能直接接入后依赖用户发现。
- 不允许用未经量化的网页运行时滤镜补救整段视频偏色。颜色漂移应优先在生成阶段通过关闭风格化/自动调色和缩短动作时长控制；仍有偏差时，对原始视频做离线逐帧色彩匹配并输出新视频资产，再由网页直接播放。网页只负责抠绿与降级，不承担主观调色。
- 动作视频必须把角色头部和五官作为身份锚点：眼睛形状与间距、鼻口结构、脸型、耳位和头身比在所有采样帧保持一致。即梦当前最短时长为4秒，跑、跳等大动作统一优先采用4秒短循环，头部尽量固定，只驱动身体、四肢和尾巴；中间帧出现变脸、成年化、长鼻或眼距变化时必须重新生成或逐帧修复。
- 绿幕统一使用纯色 `#00FF00`，角色边缘不得残留绿边或被抠色污染。抠绿后还要复核耳尖、毛发、尾尖、手脚等细边缘，并确认颜色校正没有损伤透明边缘。
- 视频验收至少覆盖：首帧、25%、50%、75%、尾帧五个采样点；检查角色身份、色调、清晰度、肢体结构、镜头稳定、动作连续和首尾循环。任一采样点明显变色、变形或漂移时，该视频不得标记为可接入。
- 页面接入时保留静态图片降级；视频加载失败、减少动画、页面隐藏或性能不足时退回同色调静态角色，不允许出现另一套颜色或角色形象。
- 对仅持续少量帧的头顶小点、局部增生、闪点等小范围瑕疵，优先使用相邻干净帧进行局部ROI逐帧修复并羽化边缘，保留原片、音频、时长和未修复版本；修复后重新执行五点采样和完整解码检查。只有脸型、主体轮廓或大面积肢体结构失真时才要求重新生成，避免无谓消耗生成费用。
- 相邻视频默认禁止旧帧与新帧交叉混合，避免生成双猫、重影或半透明肢体；应保留上一段末帧，待下一段首帧就绪后做动作匹配切换。网页位移必须通过逐帧采样绑定到真实迈步、离地等动作时点，不能从站立或蓄力首帧开始整体平移；跳跃类位移还必须在落地站稳前结束，禁止落地后继续滑动。连续同名动作的延迟任务必须用动作级令牌隔离。
- 此后每一条宠物动作视频提示词必须包含“严格只改变既定动作，不得额外新增、删除或改变任何内容；禁止睫毛、头顶圆点、凸起、天线、饰品及新结构”，并以“每一帧动作都要连贯。”作为固定结尾，明确约束生成过程不得出现角色增生、断帧或跳帧。

### 11.1 动作链与拐角转场准入

- 验收对象不只是单个视频，还必须覆盖“前一片尾帧 → 后一片首帧”。每次新增或替换视频后，按真实随机清单和长方形路线生成相邻片段对照表；姿态、朝向、角色大小、中心位置、曝光任一不一致，都不得直接接入。
- 姿态名称必须描述真实画面，不能把正面跑姿、背面跑姿或腾空姿态笼统标成 `H1`。蓝喵至少区分 `H0`坐姿、`H1_LEFT/H1_RIGHT`侧面站姿、`RUN_FRONT_A/B`、`RUN_BACK_A/B`、`AIR`；安小居建立自己的同类状态，不能复用蓝喵姿态名掩盖差异。
- 四边跑动必须同时验收两种路线方向的8个拐角连接：右→下、下→左、左→上、上→右，以及左→下、下→右、右→上、上→左。侧面站姿与正/背面步态之间必须使用真实入场/收尾视频，禁止静态镜像、瞬间翻转、CSS旋转、整体滑动或交叉淡化冒充转身。
- 方向动作采用“入场原地转向 → 主跑片段负责网页位移 → 收尾原地恢复标准站姿”的三段式。位移开始点绑定主跑首个真实迈步帧，位移结束点早于收尾片段；视频提前结束时仍须等待网页位移完成，不能提前移除 `pet-moving` 导致瞬移。
- 每个相邻片段至少抽取前片尾部5帧与后片头部5帧，检查跳帧、亮帧、重影和身份变化；随后在正式页面按100～200毫秒采样按钮坐标，确认单轴连续移动、拐角无对角穿越、动作结束无坐标补跳。

### 11.2 新角色与单视频替换工作流

- 安小居接入前先确定独立角色基准图、`H0/H1`及各方向步态锚点，再制作动作链；不得直接套用蓝喵视频、颜色统计或脸部锚点。蓝喵与安小居使用角色隔离的素材目录和清单，但共用播放器、调度、位移同步、失败降级和验收工具。
- 新增动作只增加独立 clip 与动作映射；替换旧动作只修改该 clip 的文件、版本、裁剪和姿态契约，禁止要求其他视频重新生成。原片按角色保留在 `outputs/pet-video-source/蓝猫/`、`outputs/pet-video-source/安小居/`，网页正式片放在 `assets/pets/videos/<角色>/` 或兼容目录。
- 固定执行顺序：基准角色确认 → 动作链拆分 → 中文命名首尾帧 → 200字内提示词 → 生成视频 → 五点单片检查 → 十帧相邻接缝检查 → 离线颜色/绿边处理 → 清单注册 → 定向测试 → 正式页面随机至少5轮。任一步失败只返工对应单片。
- 宠物问题与通用解法维护在 `assets/pets/videos/README.md`；角色提示词、素材映射和验收结果分别维护在 `outputs/pet-video-source/蓝猫/宠物视频提示词汇总.md`、`outputs/pet-video-source/安小居/安小居视频提示词汇总.md`。本文件保留不可绕过的工程准入规则。

## 12. Token 与 Pro 周额度节流规则

本节约束的是无效上下文、重复工具调用和过度验证，不得以节省 Token 为由跳过第3、4、6节要求的必要验证。风险与质量要求冲突时，以正确性、安全和可回退为先。

### 12.1 上下文与任务边界

- 一个任务只承载一个主要目标。当前里程碑完成后若用户切换到明显不同的模块，先交付当前结果并建议新建任务；不得把多个无关的大型开发长期堆在同一任务中。
- 长任务按可独立验收的里程碑推进。完成一个里程碑后只保留“目标、已改文件、关键决策、测试结果、未完成项”的短摘要，不反复读取或复述旧日志、整份文件和历史截图。
- 单轮预计超过40次模型/工具往返，或出现连续5次修补仍未通过时，必须先停下来做一次根因归纳并合并下一批操作，禁止靠重复试错继续消耗。
- 自动上下文压缩应在明显低于模型最大窗口时触发；压缩后必须保留当前工作区状态、用户未提交改动、验收标准和风险边界，不能以丢失任务事实换取节省。

### 12.2 读取、搜索与工具输出

- 默认先用 `rg`、定向行段、字段投影和统计摘要定位；禁止为了找一个入口输出整份大文件、完整 JSON、完整数据库记录或完整构建日志。
- 普通命令输出默认控制在4,000 tokens以内，搜索/清单默认2,000 tokens以内；只在结果被截断且缺失内容确实影响判断时扩大。测试失败先看失败用例与末尾日志，不重复打印成功项。
- 能在一次调用中并行完成的独立只读检查应合并；不得把可批量读取的十个文件拆成十轮模型往返。对外部网页、截图和浏览器状态只读取本次判断所需区域。
- 工具产出的长文本先在本地做计数、筛选、哈希或结构化摘要，再把最小证据送入上下文。禁止把大型二进制、Base64、整页 DOM、全量会话记录或超长接口响应直接回灌模型。

### 12.3 修改、浏览器与测试节流

- 遵循“定向检查一次 → 合并补丁一次 → 最小验证一次”的默认节奏。只有测试或真实页面给出新证据时才进入下一轮补丁，避免无证据的微调循环。
- 浏览器验证按“正常态、刷新恢复、失败态、重复操作”设计一次性脚本或清单；同一未变化状态不重复截图、刷新和读取。视觉问题只截取相关区域，除非必须验证全页布局。
- 低风险和中风险改动严格执行第4节的分级测试。全量测试默认最多在最终候选上运行一次；只有该次全量测试之后又修改了公共接口、高风险链路或失败相关代码，才允许再次运行。
- 外部 API 验证遵循固定检查链，已有本地材料或权威结果时不做宽泛联网搜索；普通核查默认2至4个定向查询，证据足够即停止。

### 12.4 模型、回复与额度阈值

- 日常文案、检索、样式和局部代码默认使用满足任务的最低合理推理强度（low/medium）；公式、鉴权、迁移、并发、复杂根因和最终高风险复核才提升到 high/xhigh。不得把最高推理强度作为所有任务的默认值。
- 中间更新和最终交付只报告结论、证据、改动、验证与边界，不粘贴大段工具日志，也不重复用户已经看到的内容。
- 周额度剩余低于50%时，主动拆分大型任务并优先局部验证；低于25%时暂停非必要的全仓审计、重复视觉生成和宽泛调研，并明确提示用户保留额度给关键修复。必要的安全、数据和发布验证不受此阈值削减。
- `scripts/analyze-codex-usage.mjs --summary` 仅在用户要求用量复盘、额度异常或大型阶段结束时运行；不得每轮扫描全部历史会话。复盘采用缓存读取0.1倍、缓存写入2倍、输出5倍的近似有效用量口径，同时保留原始 Token，避免把缓存命中误报成等额消耗。

### 12.5 每轮结束前节流自检

- 是否读取或输出了与当前判断无关的大文件？
- 是否重复运行了同一全量测试或重复验证未变化页面？
- 是否可以把后续独立工作放到新任务，避免继续放大旧上下文？
- 是否选择了与风险相称的推理强度、测试范围和输出长度？
- 是否既节省了 Token，又完整保留了必要证据、用户数据和回退能力？
