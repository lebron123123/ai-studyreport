# 全篇要求与小节业务逻辑分层：技术验收

日期：2026-09-05；适用版本：当前本地未提交工作树。保留用户此前全部修改，不提交、不推送。

## 可见结果与操作

1. AI可研右侧报告第一章之前新增“全篇要求”，统一展示语言、称谓、连续编号、事实边界与多点位写作策略；不增加报告章节、不写入成果正文。
2. 后台“可研逐小节生成逻辑 → 改造项目 → 住房改造”在章节清单之前显示同一全篇要求。通过原“修改生成逻辑”权限入口编辑并发布；沿用现有审核、版本与历史发布流程。其他场景独立保存，不串用住房规则。
3. 小节中的显式“全篇执行：”重复段移出，保留该小节所需材料、业务分析方法、表格字段等专属要求。不机械删除业务段内有具体对象的核验/表格约束。
4. 生成提示词统一注入全篇要求，包括未匹配到单节规则的情况。后台发布后，前台重新加载逻辑供后续生成使用；已有正文仍按受影响候选稿确认，不自动覆盖。

## 数据与兼容

- 新增场景级 `globalRequirements` 字段；`report-writing-policy.js` 是 API、前后台和历史快照唯一转换实现。
- 老数据库记录读取时无副作用投影，不批量覆盖正式规则；下次正常发布时保存新字段。显式清空优先于旧段落，不会从历史规则重新填回。
- 旧快照比较先做同义归一化：仅搬移要求不触发重生成；真的改变全篇要求才纳入影响判断，并保留锁定正文。
- 基础规则74项；住房规则64项、12章41小节，ID与业务排序不变。
- 当前本地数据库真实发布记录为v5，缺少独立logicVersions字段；不冒充已经人为发布v19/2.0。隔离页面使用仓库种子v19/2.0，测试修改升为2.1/v20；该发布只写验收页内存，不写真实数据库。

## 改动文件

- 转换、生成、影响判断：`report-writing-policy.js`、`report-logic-core.js`、`project-workflow.js`、`functions/api/reportlogic.js`。
- 展示与后台编辑：`report.js`、`aireport.js`、`report-logic-admin.js`、`index.html`、`admin.html`。
- 正式种子与一致性清单：`data/report-logic-gaibao-v1.json`、`functions/api/_reportlogic-gaibao-seed.js`、`data/release-manifest-v1.json`、`functions/api/_release-manifest.js`；均通过既有生成器同步。
- 验收：`tests/report-writing-policy.test.js`、`tests/fixtures/report-global-requirements-harness.html`；只读实际API核验脚本 `outputs/0905-global-requirements-live-check.mjs`。

## 已执行验证

- 语法检查303个JS文件通过；定向27项通过；最终全量526/526通过；发布一致性与git diff空白检查通过。
- 行为测试涵盖场景隔离、旧数据提取、显式清空、幂等、不修改输入、超长拒绝、版本递增、提示词只注入一次。
- 35节已完成样本连续3次同步：纯搬移不产生受影响标记，正文保留；真实全篇修改能识别影响，锁定正文不覆盖。
- 真实浏览器加载正式模块的隔离页面：前后台首章前展示、后台编辑/发布2.1、重新进入前台读取同样2.1与文字、空规则、读取失败提示、刷新重新加载种子通过，控制台无错误。隔离页刷新不是数据库持久化测试，不将其冒充真实税务局35节接受验收。
- 本地服务已按正常网络权限重启；站点、outlines、calcconfig、RAG查询成功，并完成真实 `/api/generate` 请求。读取真实发布API后与数据库无副作用转换逐项比对通过：64条住房逻辑无重复“全篇执行”段，真实用户数据写入0。

## 评估与边界

44维沿用同日完整基线 `0905项目企业级能力44维持续评估_对话布局与恢复核查.md`；60维以 `0903项目企业级能力60维持续评估_八项发布治理更新.md` 为战略基线，只补本次证据，不虚增历史分。

直接受影响维度：5可解释性、26提示词与版本治理、35回归测试、42管理员可维护性，增加分层、版本和浏览器证据；10任务持久化、40交互恢复验证兼容，不修改既有保存链路。其余44维无变化。战略47规则治理、51依赖失效、52规则范围/发布、58单一事实源补充上述证据，其余16项无变化。Agent专项涉及架构分层、交互、工程化和业务适配；工具、记忆、MCP、多Agent等不在范围，不为追分扩张。

能力覆盖平均分、生产成熟度加权分、真实数据资产成熟度均未重新认证；数值提升/下降均无。不以隔离页面或测试数冒充多人生产实证。

最优先三项：①在原浏览器刷新确认全篇卡片与真实项目正文完整；②下一次实际全篇要求修改按发布→影响候选→接受→刷新完成Golden项目验收；③后续补多人并发发布与历史版本回滚的生产证据。

源码统计（固定口径）：370文件，9,921,112字节，9.46MiB，78,214行。未覆盖：用户原Chrome当前打开页、远程服务器部署、真实项目批量生成/接受（未授权本次触发，未执行）。
