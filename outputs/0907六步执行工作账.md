# 六步工程执行账与本地验收边界

基线：f8a1bed；保留所有历史未提交成果。本轮用户授权本地实现，不包含推送、正式报告代签或真实项目压力测试。

## 责任边界

- 任务与预算：输入已授权项目和生成请求；输出不可变任务快照、正文候选及费用账；不直接覆盖正式稿。
- 验收与审批：输入已保存报告、检查契约和证据；输出版本绑定的检查/审批记录；机器检查不等于业务签认。
- 权限：现有三角色保留；撤销会话和停用账号统一拦截；正式审批独立指定。
- 运维：只对明确标识的系统测试项目和隔离环境执行混合负载及恢复；不伪造生产数据。

## 待办与验收

- [x] 未知调用对账、审计、幂等和管理入口；未知费用不自动算零、不盲目重发
- [x] 结构/数值/表格/引用确定性评测及原件绑定；不代替语义和财务签认
- [x] 输入快照、规则优先级、在途旧版本保留、依赖变化提示
- [x] 后台已保存正文冻结、内容哈希识别当前/历史、查看冻结正文与检查依据；不包含自动语义差异审查
- [x] 登录撤销/账号停用检查及在途任务撤权
- [x] 独立审批、禁止自审、正文变化后旧审批仅作为历史记录
- [x] 流式原件存储、哈希下载与备份隔离恢复
- [x] 小规模混合负载、内存指标和手动告警测试工具；非持续SLO服务
- [x] 定向行为测试、隔离数据库、迁移、全量回归
- [x] 正式服务及新增浏览器路径验证；非全站所有浏览器路径验收
- [x] 44/60维评估、源码统计、边界清单

## 当前进度

- 本次为本地实现与工程验证完成，不宣告六步的真实业务/生产验收全部完成。未提交或推送Git。
- 修正集成发现的旧表重名：原有Project Brain的`project_artifacts`保持不动，新原件模块改用`report_source_artifacts`，启动时顺序初始化，避免首次并发创建冲突。
- 前台：左侧“验收与运行保障”，有报告时右侧同名按钮；只读成员也有入口。原件上传后显示清单，可鉴权下载；刷新重新从后台取数。
- 只允许项目所有者冻结；复核人必须是另一位有效只读成员，且无该项目编制/编辑历史；浏览器未替真实人员签认。
- 验收使用后台已保存正文，不会把未保存编辑悄悄当作正式版本。操作前先保存。

## 修改文件与责任模块

- 任务/评测/费用：`functions/api/_agent-budget.js`、`agentbilling.js`、`_report-quality.js`、`_report-trusted-evaluation.js`、`reportexecution.js`。
- 鉴权：`_account-security.js`、`accountsecurity.js`、`_auth.js`、`auth.js`、`_agent-policy.js`。
- 验收/文件：`_delivery.js`、`reportdelivery.js`、`projectartifacts.js`、`local-server/rag-object-store.js`。
- 运维：`operations.js`、`local-server/runtime-metrics.js`、`static-policy.js`、`server.js`、`migrations/0022_delivery_security.sql`。
- 页面：`report-delivery-ui.js`、`aireport.js`、根目录`auth.js`、`index.html`；发布清单两处已生成同步。
- 测试/工具：`tests/delivery-quality.test.mjs`、`delivery-postgres.test.mjs`、`report-execution-postgres.test.mjs`、`projects-save-size.test.mjs`、`report-feedback-trust.test.mjs`；`scripts/verify-delivery-database.mjs`、`delivery-live-smoke.mjs`、`delivery-mixed-load.mjs`。
- 历史用户文件`_reportlogic-seed.js`未纳入本轮修改。

## 实际验证（2026-09-07）

- `npm test`：546项，541通过，5条件性跳过，0失败。新增高风险PG行为另在隔离数据库执行16项，全部通过、无跳过。
- 22项迁移：空库、重复初始化、旧结构升级通过；新原件API验证与旧Project Brain表共存。
- 正式localhost：两账号22次混合读写零失败，P95=448ms；上传/下载、跨账号403、私有路径404、真实`/api/generate`通过。这个样本不能推算服务器容量。
- 持久化生成任务完成后3次重读一致，单条费用账且已结算。
- 浏览器：空项目入口刷新可见，空验收/原件清单、无复核人失败提示、关闭重新进入通过；控制台error为空。未操作真实报告正文或真实审批。
- 最后页面小改后：`node --check aireport.js`、`node --check report-delivery-ui.js`、定向4项测试通过；`git diff --check`无补丁空白错误，仅CRLF提示。
- 正式服务以正常网络权限启动，AI Provider连通探测及真实生成通过；当前服务保持运行。
- 新备份：`.tmp/protected-backups/2026-09-07T08-05-57-291Z`，82文件、134170062字节。
- 隔离恢复：`studyreport_restore_1788768418975`，110表、37项目、81对象文件哈希核对通过；原正式库未覆盖。状态为可读恢复，非业务负责人批准。
- `[系统测试]`项目、测试账号、隔离数据库与备份为验收证据保留，未擅自删除；早期失败测试批次也保留。
- 源码统计：409文件、10095359字节、9.63MiB、79920行（固定排除规则）。

## 仍需投入而非本轮完成的部分

1. 正式Golden和独立Holdout：目前有执行器与冻结工具，但未自动把冻结记录绑定为负责人批准的Golden；需要真实样本、业务确认和完整复核。
2. 常驻告警与监控：本轮仅有指标读取、手动HTTPS告警测试；未启用自动调度、告警持久留存、真实接收验证。需要确定机房监控平台和接收人后接通。
3. 长时间多Worker、大模型混合容量、异机灾备及高可用仍未验收；现有小规模测试和本机隔离恢复不能代替。
4. 原件接口单文件上限256MiB，是可控保护，不是无限容量；历史材料不自动重新归档。未来机房将元数据放数据库、原件放持久对象目录或对象存储，并做异机备份；不是放浏览器作为唯一副本。
5. 不是对全站所有旧入口作了统一权限/恢复重写；其他模块和跨组织完整矩阵仍需专项验收。没有因此把60项分数上调。

## 不可代替的正式验收

负责人签认的基准报告/测算、独立留出项目、真实账号职责、机房容量/告警接收和持续运行记录。合成测试不能替代。

## 2026-09-07 追加：可继续推进的四项代码

以下记录替代上文“仍需投入”中关于本轮代码尚未接入的描述；上文保留为阶段历史。

1. **Golden绑定**：受控样本必须引用已由独立复核人通过的冻结版本，记录正文哈希、复核人与时间、检查合同及批准正文。登记时校验当前正文；执行及结果写入时再次校验来源权限。不能用一段“已批准”备注冒充真实审签。训练/留出集合在同场景按项目、材料及版本哈希阻止交叉污染，事务锁避免并发绕过。旧备注型样本保留，但不再作为可信评测依据。管理员在验收版本内登记；系统无法自动证明两个不同项目在业务语义上绝对独立。
2. **自动监控**：本地服务默认每60秒检查运行状态，告警及恢复事件写入数据库；HTTPS接收失败会重试，未配置接收地址时明确显示未配置而非送达。投递为至少一次，接收方需按idempotency-key去重。整机停机需外部探针，不能由自身进程证明可用。
3. **旧入口权限**：reportorchestration从已持久化记录解析真实项目，不信任请求伪造projectId；查看/编辑权限统一检查，写入事务锁定项目，移除成员后读取被拒绝。评测任务还检查样本来源项目权限。本次覆盖这些旧编排入口，不宣称已全面重写全站权限。
4. **自动演练**：新增独立子进程故障工具，强杀调用前/调用中测试进程，双Worker竞争接管，验证旧Worker结果被租约拒绝、调用中断后不盲目重发模型。混合负载工具可显式加入真实模型调用，默认不开启付费调用。

### 本轮责任文件

- Golden：`_report-case-provenance.js`、`_report-trusted-evaluation.js`、`_agent-enterprise.js`、`report-delivery-ui.js`。
- 权限：`_report-route-access.js`、`reportorchestration.js`。
- 监控：`_operations-monitor.js`、`operations.js`、`local-server/operations-monitor.js`、`server.js`、`migrations/0023_operations_monitor.sql`。
- 演练/验证：`recovery-drill.mjs`、`recovery-worker-fixture.mjs`、`delivery-mixed-load.mjs`、`delivery-live-smoke.mjs`、`verify-delivery-database.mjs`及对应3组PG测试；发布清单已重新生成并检查。

### 本轮最终证据

- 全量：550项，543通过，7条件跳过，0失败；数据库专项20项全部通过无跳过。23项迁移在空库、重复执行和旧库升级路径验证通过。
- 语法检查345份JS通过；发布一致性检查通过；git diff --check无错误（仅既有LF/CRLF提示）。
- 故障演练在隔离恢复库的独立schema中通过：真实子进程终止、双进程竞争、迟到结果拒绝、调用中终止不重复调用。使用生产核心claim/execute/settle函数及模拟Provider，不等于机房多实例长期压测。
- 正式localhost已重启为PID15568，默认Provider网络可达；自动监控已启动。两测试账号24次混合请求零失败、P95=1788ms，包含2次真实模型生成；独立生成、下载、跨账号403及私有路径404也通过。
- 浏览器：正式站刷新及验收空态无控制台错误。隔离页面加载真实UI脚本，已审签分支的样本登记错误态、成功反馈、关闭重入、刷新检查通过；API结果为模拟数据，真实后端由PG专项验证，未代替用户完成真实审签。
- 测试账号、文件和隔离schema留存作为证据（包括早期失败批次），未修改税务局正文。未提交或推送Git。
- 当前有效源码与结构化配置：418文件、10,122,786字节、9.65MiB、80,202行。

### 部署配置与余留边界

`OPERATIONS_MONITOR_ENABLED=false`可关闭调度；`OPERATIONS_MONITOR_INTERVAL_MS`设置周期（默认60000，最小10000）；`OPERATIONS_ALERT_URL`配置HTTPS接收端，修改环境配置后重启。告警事件在PostgreSQL的operations_monitor/operations_alerts表留存。未配置真实接收方时不会声称通知已送达。

四项本轮实现与上述验证已经完成；正式Golden/独立Holdout材料确认、真实通知接收、长时间机房容量和异机灾备仍需业务及部署条件。继续沿用三类评分，不凭代码增量虚增分数。优先动作：真实基准与留出样本签认、接通告警接收端、机房阶梯负载及异机恢复。
