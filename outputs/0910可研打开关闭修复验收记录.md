# 0910 可研打开关闭修复验收记录

状态：三个入口补丁已落地，税务局真实页面和编辑入口已验证；原龙岗页面存在真实保存版本冲突，保留原页不刷新，未宣称全部闭环。

## 后续复核（同日）

- 正式项目“打开项目”已在浏览器进入税务局项目。仅浏览管理覆盖层不再等待底层研究保存。
- 历史可研“打开可研”成功后才关闭列表；失败在原列表提示，成功清理不再恢复旧路由。auth.js延迟恢复绘制，避免同一长报告重复绘制。
- 浏览器已显示税务局报告标题、43小节和对应资料，不是龙岗正文。先前自动化连接超时不能单独作为页面卡死证据。
- 候选范围无受影响小节时显示说明并禁用继续；选择全部后继续可用。浏览器确认；未实际发起整篇生成。
- 税务局首节“AI修改”已打开修改输入框并取消，未改正文。
- 研究保存冲突只在远端内容与基线或待保存内容完全一致且身份、轮次、权限仍有效时重试；内容不同仍拒绝覆盖。新增行为测试覆盖上述分支。
- 原龙岗页重试保存返回“已有更新”，原页仍保留，不刷新、不丢弃、不强行覆盖。要继续该页切换须先保留并核对本机与服务端差异。
- 本次44/60维新评估未完成，不以历史分数冒充新评分，成熟度暂不上调。

## 本次范围

- project-manager.js：关闭覆盖层不等待底层报告保存，真正切换项目的保存保护不撤销。
- research-session.js、research-ui.js：增加无正文的身份快照；减少权限检查的整稿复制；恢复草稿延迟到最终模式统一绘制。
- report.js、aireport.js：恢复时支持延迟绘制；初始卡片展开不触发保存，真实展开收起仍保存。
- functions/api/research.js、_research-store.js：研究状态上限20MiB，请求允许额外协议开销，超限拒绝且保留旧状态。
- index.html：更新静态脚本版本；AGENTS.md补充上述准入规则。

## 已执行验证

- 最新全量 npm test：783项，757通过、0失败、26跳过；21.337秒。跳过项不计通过。
- 新增行为测试：轻量身份不含正文且不可反向修改会话；长正文保存；超限拒绝保留旧状态；初始卡片展开不保存；面板关闭不等待报告保存。
- 变更JS语法检查通过。
- 本地服务已重启至最终API；站点、研究列表、大纲接口200，真实生成接口返回“正常”。该生成请求仅做连通性验证，未写入用户报告。
- 浏览器从“我的项目”选择“龙岗布吉盈信大楼”，URL正确进入指定研究；随后读取页面/错误日志连续连接超时，无法确认长报告打开及X关闭的实际响应。该项未通过，不以URL或单测代替验收。

## 未完成与下一步

1. 获取该长报告打开后真实可见状态，区分浏览器检查连接故障与页面本身卡顿。
2. 实测打开、关闭、返回再进入、刷新及错误态；如仍卡顿，继续定位实际耗时，不扩大重构。
3. 完成44维和跨模块60维本日评估前不发布完成声明。当前未形成可操作验收证据，不上调成熟度分数；历史综合分不能当作本次新评估结果。

未提交或推送Git；未删除项目、报告、正式资料；不触发全篇重新生成。
