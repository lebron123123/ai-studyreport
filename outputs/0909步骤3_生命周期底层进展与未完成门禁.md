# 步骤3：生命周期底层进展与未完成门禁

日期：2026-09-09；基线 HEAD 3953dda 上的本地未提交工作树。

## 结论

**步骤3尚未完成，不得标记为可供用户操作。** 本次完成的是底层增量，未接入旧 AI 可研页面。未迁移用户数据库、未开启 RESEARCH_IDENTITY_ENABLED、未重启服务、未提交或推送 Git。

## 实际实现

- 研究及轮次列表按私人所有权/有效共享成员权限过滤。
- 空白重新开始保存旧轮次，不删除旧会话；共享重启要求管理角色与服务端二次授权。
- 废弃、恢复保存幂等操作记录；所有轮次 epoch 递增，恢复不使旧请求重新有效。
- 本人密码校验、共享管理员范围校验、凭据更新后的证明失效；一分钟最多5次验证，失败次数单独提交，不能随业务回滚而消失。
- 显式 ResearchSession 适配器：保存串行CAS、保存失败阻止离开、响应丢失后重放同一请求、七类任务使用冻结身份。
- 适配器没有替换全局 fetch、项目ID或正式项目存储，也**尚未被生产页面调用**。

## 文件

- functions/api/_research-store.js：列表、重启、废弃与恢复。
- functions/api/_research-authorization.js：二次验证与限流。
- functions/api/research.js：上述API动作。
- migrations/0035_research_reauthentication.sql：仅新增限流表。
- research-session.js：显式页面适配器。
- tests/research-identity.test.mjs、tests/research-session.test.cjs：行为验证。
- data/release-manifest-v1.json、functions/api/_release-manifest.js：迁移清单更新为35项。

## 验证证据

1. 变更 JS 语法检查通过。
2. SQLite行为验证通过；随后在独立 PostgreSQL 库验证真实事务、并发与隔离。
3. 35项迁移空库、重复执行、升级检查通过。
4. 最终候选运行 `node --env-file=local-server/.env scripts/run-postgres-tests.mjs`：**777通过、0失败、0跳过**。临时隔离库已移除，未删除用户数据。
5. `node scripts/check-release-consistency.mjs` 通过。
6. 浏览器主流程、真实生成与导出未验收，不能用纯适配器测试代替七条实际链路测试。

全量日志：outputs/0909-research-step3-regression.log。

## 已定位的剩余接入责任

|责任模块|当前旧行为|必要接入|
|aireport.js / auth.js|重新开始删除旧会话；聊天/自动保存仍依赖 currentProjectId|新增研究入口、历史轮次只读、生命周期按钮、研究专用保存与刷新恢复|
|report.js|正文与材料使用旧项目全局上下文|完整研究快照及每次异步结果应用前的身份校验|
|reportexecution.js / _agent-enterprise.js|生成任务只按正式 projectId 复核权限|服务器持久任务绑定 researchId/runId/epoch，开始与完成时重新核验|
|export.js|导出读取现有可变工作稿|冻结本次研究轮次快照及身份校验，避免切换后导出错稿|
|资料与事实继承|verified模式缺少现有资料库校验适配|重新校验来源、权限、效力及版本；当前拒绝此模式，不伪造继承成功|

不得为了让新按钮可点，把研究ID冒充正式项目ID，或绕过已有任务权限与持久化机制。

## 下一次

**仍然继续步骤3**：完成上述页面与服务端七链接入，然后真实演示“新增—编辑—重新开始—旧轮次—废弃—恢复”，覆盖刷新、返回、重复点击、错误态、双用户与迟到任务；通过之后才能开始步骤4布局。

## 统计与评分

当前有效源码及结构化配置：557文件，11,691,521字节，11.15 MiB，110,293行。按仓库固定排除口径统计。

44维沿用同日完整基线，未接入主流程的新增能力不涨分：覆盖73.84、生产加权74.92、真实资产代理64.33（非全量资产实测）；60维历史平均74.30。无提升/下降项，不构成当前生产认证。
直接影响端到端闭环、安全鉴权、细粒度权限、会话隔离、审计、回归与交互恢复；本次证据加强隔离模块，但尚未证明生产行为。
前三项行动：①接通真实七链；②真实页面完整演示；③完成后再按批准范围备份/迁移/启用并验证服务主链路。
