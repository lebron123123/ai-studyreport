# 项目企业级能力44维持续评估（蓝猫视频随机工作流接入）

日期：2026-08-30
复用完整基线：`0830项目企业级能力44维持续评估_蓝猫待机视频接入.md`
综合分：**84.8/100（无变化）**

本次已把10个动作、18段视频接入网站，但不改变可研、测算、RAG、权限或真实项目能力，因此综合分不虚增。完整44维分数沿用同日基线；以下仅更新有新证据的维度，其余36维均标记“无变化”。

| 维度 | 基线分 | 本次证据 | 主要缺口 / 下一步 |
|---|---:|---|---|
| 可观测性 | 78 | 页面暴露当前动作、分段、姿态、动作池余量和播放器状态 | 增加长期帧耗时与视频失败率统计 |
| 离线评测 | 83 | 起身按首/25%/50%/75%/尾采样核查；18段建立统一清单 | 建立自动逐帧身份和亮度阈值检测 |
| 回归测试 | 98 | 语法检查及8项视频工作流测试通过，浏览器实际观察到跳跃、跑步、伸展、挥爪与姿态转换 | 登录态下补完整点击问答E2E |
| 部署可移植性 | 80 | 仅依赖浏览器原生Video/Canvas，无新增运行依赖 | Safari与低端设备实测 |
| 交互恢复 | 97 | H0/H1自动补坐下/起身；问答面板打开时停止视频；经典32/16帧仍封存 | 弱网中断后的断点恢复 |
| 降级容错 | 89 | 视频失败或减少动画时显示同一蓝猫静态素材；源视频完整保留 | 缓存损坏和断网测试 |
| 管理员可维护性 | 97 | 18段视频独立注册，单段可单独换文件、版本及裁头裁尾参数 | 后台可视化素材管理 |
| 成本控制 | 76 | 不调用模型；视频按动作顺序按需解码，旧帧不并行运行 | 素材压缩/CDN缓存 |

## 本次结论

- 提升项：交互恢复、降级容错、可维护性和回归证据增强，但保持原分数，等待真实登录态长期运行数据。
- 下降项：无。新增82.11MiB静态视频资产，属于部署体积风险，不影响模型Token成本。
- 最优先行动：①登录态连续运行30分钟；②按每段实际亮帧长度微调裁剪参数；③后续新增安小居视频时复用同一清单协议。

## 补充修复：静态停顿、休息消失与镜像方向

- 动作完成不再依赖预估定时器，而以整组视频真实播放完成事件为准，避免后续动作提前中断上一组分段。
- 动作间隔由6～10秒缩短为1.2～2.6秒；加载下一动作时保留上一视频画面，不再短暂切成旧PNG。
- 休息、问答和页面隐藏时暂停解码但保留当前画布；没有可用画布时才显示同色静态降级素材。
- 跑步在首个真实视频帧就绪后才开始页面位移；静态动作随机左右镜像，跑步镜像与移动方向绑定。
- 相关10项模块测试及浏览器连续采样通过。综合分仍保持84.8/100，提升交互恢复与降级容错证据，不调整业务能力分。

## 补充修复：动作永久停留

- 根因：视频结束瞬间若 `canAct()` 因页面点击、鼠标悬停或650毫秒静默期返回否，旧流程会把定时器清空后直接进入休息态，没有恢复入口。
- 修复：视频动作结束后始终建立下一动作调度；暂时不能行动时每450毫秒重试，用户结束操作后自动恢复。
- 每个分段新增媒体结束事件和6秒看门狗，解码结束回调失效时也能退出当前分段。
- 浏览器16秒连续采样实际经过待机、挥爪两段、短间隔、起身和伸展，媒体时间持续推进且错误码为0；11项相关测试通过。

## 补充修复：跑跳原地踏步与换向方案校准

- 0.46秒压缩式网页假转身经用户实测不自然，现已撤回；换向改为两段真实视频：H1朝右到H1朝左、H1朝左到H1朝右。视频生成接入前暂时锁定朝向，避免继续出现直接镜像跳帧。
- 跑步、跳跃均声明为屏幕位移动作；若动作前需要“起身”，位移会等到真正的起跑/起跳首帧才开始，而不是随过渡片段提前移动。
- 首次跑跳若桌宠已经靠近屏幕边界，不再只吸附最近节点，而是直接移动到相邻边界节点，确保肉眼可见的长距离位移，并继续沿矩形边界避开页面中央内容。
- 固定跳跃浏览器采样经过 `rise → jumpUp → jumpDown`，无媒体错误，动作阶段横向移动489px；固定跑步经过 `rise → runStart → runLoop`，无媒体错误，采样阶段横向移动508px。
- 跑跳位移相关 JavaScript 语法检查、13项桌宠视频测试和浏览器真实路径验证通过；换向视频仍待生成和五点采样验收，因此不把换向能力标记为完成。综合分仍为84.8/100，不虚增业务能力分。

## 补充修复：H1→H0与H0→H0片段边界闪跳

- 核查确认H1→H0计划中已插入正式`cat-sit-v1.mp4`，不是坐下视频漏调用；视觉上的“直接跳到H0”来自相邻MP4边界硬覆盖。
- H0→H0同样会因不同生成片段在曝光、角色位置和毛发边缘上的微差，在第一帧覆盖旧画布时产生闪跳。
- 播放器现在于每次clip切换时抓取旧画布，并在0.24秒内逐帧混合到新片段；保留原有首尾0.14秒亮帧裁剪，坐下、起身及主体动作仍完整播放。
- JavaScript语法检查和14项桌宠视频测试通过；新增混合状态诊断`data-pet-video-blend`。完整登录页面的长时间人工视觉复核仍应在用户当前浏览器刷新后继续观察，综合分不调整。

## 补充完成：真实换向视频与五轮随机跳帧审计

- 新增`cat-turn-v1.mp4`：右→左播放原片，左→右播放同一视频的整段镜像；仅在转身完成后更新朝向并启动跑跳位移，不再直接镜像静止H1。
- 转向视频4.096秒，5%、25%、50%、75%和尾段五点采样通过，角色身份、毛色、肢体与绿幕连续，媒体错误为0。
- 五轮随机链覆盖挥爪、伸展、跑步、跳跃、警觉及H1/H0转换，实际触发`turnRightToLeft → runStart → runLoop → runStop`、`sit → idle`和`idle → idle`。
- 共记录25次片段边界混合；失败0次、静态降级0次、活动视频意外消失0次。16项桌宠视频测试通过，所有随机动作组合的H0/H1姿态链均连续。
- 综合分仍为84.8/100；提升了交互恢复、离线评测、可观测性和回归证据，但不改变可研业务能力分。

## 补充修复：连续活动与跑跳步态同步

- 动作间空档由1.2～2.6秒压缩至0.18～0.50秒；鼠标离开、拖动结束、面板关闭和页面恢复后的等待同步压缩至0.32～0.70秒。
- 加权动作池共12张动作牌，跑步3张、跳跃3张，跑跳合计占50%；跑步冷却由10～15秒降至2.5～4.5秒。
- 跑步页面位移仅绑定`runLoop`，`runStart`与`runStop`不再拖动容器；跑跳均只做水平移动，画面上下腾挪由原视频内的四肢和身体承担。
- 浏览器按秒采样验证：跑步`runStart`连续4次采样位置不变，`runLoop`横向移动540px且纵坐标始终540px，`runStop`位置稳定；跳跃进入`jumpUp`后开始水平移动，纵坐标保持540px，媒体失败0次。
- 17项桌宠视频回归测试全部通过。综合分仍为84.8/100；回归测试、交互恢复、可观测性证据增强，但不改变可研、测算和知识库业务能力分。

## 补充修复：高速虚影、跳跃平移与长方形路线接口

- 跑步、跳跃分段禁用0.24秒交叉淡化，改用动作匹配切换，消除两个高速姿态叠加形成的两帧虚影；静态动作继续保留淡化以抑制边界闪跳。
- 跳跃取消网页容器位移。浏览器连续采样`jumpUp → jumpDown`期间坐标始终为`left=1110, top=540`、`moving=false`、媒体失败0次。
- 跑步改为核心内容外侧长方形路线协议，当前左右素材沿上/下完整长边运行；浏览器采样从`left=1110`移动到`left=18`，纵坐标始终540，且仅`runLoop`阶段移动。
- 已注册上下方向素材槽位，但明确标记`verticalAssetsReady=false`。现有侧视素材不能通过镜像生成正面/背面信息，禁止旋转90°或侧面上下滑动伪装；待新增向上/向下视频后即可补齐左右短边而不改其他动作。
- 17项相关回归测试及正式运行页面冒烟验证通过。综合分维持84.8/100；提升交互恢复、可观测性、回归测试和管理员可维护性证据，无下降项。
