# AI可研生成功能：GitHub优秀产品对标与更新建议

> 编制日期：2026-08-11  
> 分析范围：当前本地代码、AI可研生成、白箱测算、RAG/Wiki、正式资料台账、智能审查、项目库、AI办公助手及其工具调用框架。  
> 本文件只提出产品和实施建议，尚未据此修改功能。

## 一、先说结论

当前系统已经不是一个简单的“输入一句话，AI写一篇报告”的工具了。它已经具备：项目信息抽取、周边调研、参数治理、确定性财务测算、逐节生成、RAG引用、Excel数字溯源、智能审查、项目保存和Word/Excel导出等较完整的基础能力。

但是，从真正编制可研报告的使用过程看，当前最明显的短板发生在**第一版报告生成以后**：

- 测算参数改了，系统不知道应同步更新哪些表格、指标和章节；
- 文字可以逐节修改，但没有修改前后差异、接受/拒绝和版本回退；
- 对话可以保存一部分，但刷新后不能完整恢复到可继续操作的工作状态；
- AI办公助手已经拥有不少查询工具，但这些工具尚未真正进入可研报告的持续修改流程；
- RAG已经能在生成时提供资料，但使用者还不能直观看到“这段话用了哪份资料、哪一页、哪个Excel单元格，以及资料更新后哪些内容过期”。

因此，下一阶段不建议再建设一个新的“通用Agent”或重做RAG底座，而应把现有能力合并成一个：

> **以项目为中心、可持续对话、测算与正文联动、每次修改可预览、可确认、可回退、可追溯的可研工作台。**

最值得优先完成的主链路是：

> 用户提出修改 → 系统识别修改对象 → 先预演、不直接覆盖 → 展示测算变化和受影响章节 → 用户确认 → 只更新受影响内容 → 查看文字差异 → 接受或撤销 → 自动复核 → 形成新版本。

这条链路比继续增加模型、增加更多按钮或照搬一个完整的工作流平台更有价值。

---

## 二、当前系统已经做到了什么

### 2.1 第一版可研生成能力已经比较完整

当前“AI可研生成”已经可以完成：

1. 从自然语言中抽取项目信息；
2. 补充周边、竞品、需求和定位资料；
3. 推荐测算参数并让用户确认；
4. 调用白箱计算引擎得到IRR、NPV、收入、成本、现金流等确定结果；
5. 按章节逐节生成正文；
6. 在数据章节中注入真实测算结果；
7. 调用RAG、正式资料、Excel精确来源和审查规则；
8. 进行规则审查和AI审查；
9. 保存项目并导出Word、Excel等成果。

换句话说，**“从零生成第一版”已经不是现阶段最弱的一环**。

### 2.2 已经具备的编辑和保存能力

当前报告正文支持：

- 直接在页面中手工编辑；
- 对单个小节输入要求并让AI修改；
- 单独重写一个小节；
- 编辑后保存本地草稿并同步项目库；
- 从“我的项目”重新打开项目；
- 审查问题点击后定位到对应小节。

这些功能非常有用，不应该推倒重做。下一阶段应在其上增加版本、差异、联动和对话工具。

### 2.3 当前仍然存在的实际缺口

| 使用场景 | 当前表现 | 实际问题 |
|---|---|---|
| 修改租金、出租率、建设期等测算参数 | 可以回到测算页重新算 | 算完后不知道哪些正文已经失效，也不会选择性更新 |
| 修改一段文字 | 可直接编辑或逐节AI修改 | AI修改后直接替换，没有修改前后差异和接受/拒绝 |
| 修改全篇口径 | 只能逐节处理 | 没有全篇影响分析和批量修改方案 |
| 刷新后继续AI可研对话 | 能恢复部分聊天和抽取结果 | 生成确认、交付等动作态会退化成普通文字，无法从原节点继续 |
| 管理多个AI可研任务 | 项目库可以保存多个报告项目 | AI可研对话存档仍是“每个用户覆盖式一份”，项目和对话没有真正绑定 |
| 查看依据 | 生成时会调用RAG和Excel来源 | 编制页面缺少直观的章节证据面板、引用锁定和过期提醒 |
| 修复审查问题 | 可以定位问题 | 不能直接形成“建议修改—查看差异—确认应用—只复核此项”的闭环 |
| 发布正式版本 | 可以导出 | 没有明确绑定“哪版参数、哪版正文、哪批资料、哪次审查结果” |

---

## 三、GitHub优秀产品有哪些值得参考

本次没有寻找一个“完全一样的可研产品”，因为开源项目中很少有同时覆盖中国可研、财务白箱测算和正式审查规则的产品。更可行的方式是分别借鉴成熟产品在文档协作、工作流、知识证据和Agent工具上的长处。

### 3.1 Open Canvas：最值得借鉴“报告生成后的持续修改”

项目地址：[langchain-ai/open-canvas](https://github.com/langchain-ai/open-canvas)

值得参考的设计：

- 对话和正在编辑的文档并排存在；
- 用户可以从现有文档开始，而不是每次必须从聊天重新生成；
- 支持常用“快捷操作”；
- 每个文档成果都有版本，可以查看以前版本；
- 可以通过对话更新当前成果。

对本项目最有价值的转化：

- 报告生成以后，对话框仍然保留，并自动知道当前项目、当前参数版本和当前报告版本；
- 选中一段话后，可以执行“更正式”“压缩”“补充依据”“与最新测算一致”等快捷操作；
- 每次AI修改先形成一个候选版本，不直接覆盖正式正文；
- 可以对比上一版并随时恢复。

不建议照搬的部分：Open Canvas是通用写作工具，不理解可研的财务依赖关系，因此需要加入本项目独有的“参数—指标—章节”联动。

### 3.2 Dify：最值得借鉴“任务状态、局部重跑和可观察流程”

项目地址：[langgenius/dify](https://github.com/langgenius/dify)；相关能力说明：[Knowledge Pipeline与队列式图引擎](https://github.com/langgenius/dify/discussions/26138)

值得参考的设计：

- 每一步的输入、输出和中间状态都能看见；
- 任务可以从某一节点开始、局部重跑、暂停和恢复；
- 长任务由队列管理，不依赖浏览器页面一直打开；
- 单个节点可以独立测试，便于发现哪一步出错。

对本项目最有价值的转化：

- 不需要照搬Dify的自由拖拽画布；
- 可以在项目中显示固定的可研流程时间线：资料就绪 → 参数确认 → 测算完成 → 报告生成 → 审查 → 签发/导出；
- 某一章节生成失败时只重跑该章节；
- 修改一个参数时，只重算及重写受影响部分；
- 关闭页面后，长篇生成任务仍可继续，回来后查看结果。

### 3.3 RAGFlow：最值得借鉴“证据透明和人工干预”

项目地址：[infiniflow/ragflow](https://github.com/infiniflow/ragflow)

值得参考的设计：

- 复杂文档解析和多种资料格式；
- 用户能看到切片和引用；
- 回答可以追溯到关键资料；
- 切片或证据不合适时允许人工干预；
- 支持更完整的知识处理流程。

本项目已经拥有RAG、Wiki、正式资料台账、Excel单元格级来源和版本治理，因此不必替换为RAGFlow。更值得做的是把后台已有的证据能力展示到报告编辑界面：

- 每个小节右侧显示实际使用过的政策、案例和Excel来源；
- 点击来源可查看原文片段、页码或单元格；
- 用户可将某条依据“固定”，重写时必须继续使用；
- 正式资料更新后，使用旧资料的小节显示“依据已更新，建议复核”。

### 3.4 302 AI Document Editor：最值得借鉴“低门槛文字操作”

项目地址：[302ai/302_document_editor](https://github.com/302ai/302_document_editor)

值得参考的设计：

- 类Word的文档编辑体验；
- 对段落进行重写、扩写、压缩、插入等操作；
- 全文和段落级摘要；
- 模板化文档处理。

对本项目的启发不是增加翻译、配图等泛办公功能，而是增加符合可研工作的快捷操作：

- 改为正式可研文风；
- 保留原数字，只优化表述；
- 补充政策依据；
- 与测算结果校准；
- 缩短重复表述；
- 补齐风险和实施建议；
- 把审查意见改成合格正文。

### 3.5 Khoj等个人Agent产品：最值得借鉴“项目级上下文”

项目地址：[khoj-ai/khoj](https://github.com/khoj-ai/khoj)

值得参考的是：Agent不只是拥有一个聊天记录，而是绑定特定知识、个性、工具和持续上下文。

对本项目而言，应把“通用用户记忆”和“当前项目事实”严格分开：

- 用户记忆：偏好的报告文风、常用称谓、导出格式；
- 项目事实：本项目地址、面积、租金、建设期、资料和审查状态；
- 公司规则：正式审查标准、参数兜底值和制度要求。

用户偏好可以长期记忆，但绝不能覆盖项目事实和公司硬规则。

---

## 四、站在可研使用者角度，理想流程应该怎样

### 4.1 第一版生成完成后，不应该“流程结束”

报告完成后，页面应自动进入“项目工作台”，保留三个核心区域：

1. 左侧：章节目录及状态；
2. 中间：可直接编辑的报告正文；
3. 右侧：项目对话、证据、审查问题三个标签页。

此时AI应该明确知道：

- 当前打开的是哪个项目；
- 目前采用哪一版测算参数；
- 正文是哪一版；
- 哪些章节由AI生成、哪些被人工修改并锁定；
- 每节使用了哪些依据；
- 尚有哪些审查问题。

用户继续说“把结论再稳健一点”，AI不应重新走项目信息提取，而应判断这是一次文字修改，先给出受影响章节和修改预览。

### 4.2 用户修改测算数据时，最便捷的正确流程

假设用户说：

> 把租金从45元调整为42元，看看报告要怎么改。

系统理想反应如下：

1. 识别“租金”对应哪个中文参数和内部参数键；
2. 显示当前值45、新值42、单位和来源；
3. 先进行硬规则及异常检测；
4. 调用白箱引擎做预演，但不立刻保存；
5. 展示变化前后：IRR、NPV、收入、利润、偿债指标等；
6. 列出受影响的表格和报告章节；
7. 询问用户是否采用；
8. 用户确认后形成新的“测算快照V2”；
9. 相关章节标记为“测算已变化，正文待同步”；
10. 用户选择“只更新受影响章节”或“暂时只更新表格”；
11. AI给出正文差异，用户逐项接受或拒绝；
12. 对受影响内容重新进行规则审查和AI审查；
13. 形成报告V2，并保留V1可恢复。

```mermaid
flowchart LR
    A["用户提出参数修改"] --> B["识别参数与来源"]
    B --> C["异常检测和规则校验"]
    C --> D["白箱测算预演"]
    D --> E["展示指标差异和影响范围"]
    E --> F{"用户确认采用?"}
    F -- 否 --> G["保留原版本"]
    F -- 是 --> H["保存测算快照V2"]
    H --> I["标记受影响章节"]
    I --> J["选择性更新并显示文字差异"]
    J --> K["接受或拒绝修改"]
    K --> L["局部复核并形成报告V2"]
```

核心原则是：**AI负责识别、预演、解释和提出修改；白箱引擎负责计算；用户负责最终确认。**

### 4.3 用户只修改报告文字时

建议支持四种修改范围：

| 范围 | 典型操作 | 系统反应 |
|---|---|---|
| 选中文字 | “这段太绝对，改稳健一点” | 只生成替换片段，展示差异 |
| 小节 | “把需求分析写得更具体” | 修改本小节，并保留其他内容 |
| 章节 | “第五章整体压缩20%” | 先列出拟修改的小节，再批量生成候选稿 |
| 全文 | “全文统一叫保障性租赁住房” | 先全局搜索影响点，确认后批量替换并复核 |

任何AI改写都不应直接覆盖人工稿，应提供：

- 原文；
- 建议稿；
- 修改差异；
- 接受；
- 拒绝；
- 接受后撤销。

如果某一小节已经被人工精细修改，可以标记“人工锁定”。批量更新时AI不得覆盖，只能提示该小节可能与最新测算不一致。

### 4.4 完成一版后继续对话，系统应该如何反应

继续对话不能只恢复聊天文字，还要恢复“可执行状态”。建议系统先识别用户意图属于哪一类：

1. 修改项目事实；
2. 修改测算参数；
3. 修改正文；
4. 补充或替换依据；
5. 处理审查问题；
6. 查询、解释或对比；
7. 导出某个版本。

例如：

- “刚才租金还是改回45” → 找到上一版参数并预览回退；
- “把刚才影响到的章节都更新掉” → 使用上一轮已确认的影响清单；
- “第三章先别动” → 锁定第三章，再处理其他章节；
- “为什么IRR降了这么多” → 调用测算差异工具解释，不改任何数据；
- “按审查意见把第五章修一下” → 找到对应问题，给出候选修改和差异；
- “导出修改前那一版” → 选择报告版本及绑定的测算快照后导出。

因此，后续对话必须绑定项目、版本和上一步任务，而不能只是普通聊天记录。

### 4.5 正式资料发生变化时

假设某份政策、批复或Excel底稿换了新版本，理想反应是：

1. 正式资料台账识别这是旧资料的替代版本；
2. 比较资料差异；
3. 找到报告中使用旧资料的章节、表格和参数；
4. 标记“证据过期”或“数字可能变化”；
5. 用户确认后重新抽取受影响事实和数字；
6. 只更新受影响章节并重新审查。

现有的正式资料台账、版本替换、Excel单元格溯源已经为此打好了基础，下一步重点是把这些结果接到报告工作台。

---

## 五、AI办公助手有哪些工具值得直接复用

当前AI办公助手已经使用统一的Agent工具框架，这是很好的基础。AI可研不需要另起一套工具引擎，应复用现有`AgentCore`，但为可研页面限定工具清单。

### 5.1 可以直接复用的只读工具

| 现有工具/能力 | 在AI可研中的用途 |
|---|---|
| 获取当前页面上下文 | 让AI知道用户正在查看哪个项目、步骤或章节 |
| 获取项目基础信息 | 回答项目事实、辅助正文修改 |
| 获取周边调研信息 | 修改市场、区位、需求和产品定位章节 |
| 搜索知识库 | 为正文和答复寻找正式依据 |
| 获取测算摘要 | 解释IRR、NPV、收入、成本等结果 |
| 列出历史项目 | 查找和对比同类项目 |
| 获取历史项目 | 参考以前项目的结构、参数来源和经验 |
| 获取审查问题 | 直接回答“现在还有哪些问题”并定位修复 |
| 页面导航建议 | 带用户跳转到参数、资料、正文或审查页面 |

### 5.2 需要新增的可研专用只读工具

- `get_current_project_snapshot`：取得当前项目、测算版、报告版和资料版；
- `get_calc_parameter`：按中文名查当前参数、单位、来源和确认状态；
- `preview_calc_change`：输入候选参数，仅预演测算差异，不保存；
- `get_report_outline_status`：查看每节是否最新、锁定、缺依据或有审查问题；
- `get_section_content`：读取指定章节当前稿；
- `get_section_evidence`：读取该节使用的资料、页码、片段和Excel单元格；
- `compare_project_versions`：比较两个测算或报告版本；
- `find_impacted_sections`：根据参数、资料或全局口径定位受影响内容。

### 5.3 可以修改数据的工具必须增加人工确认闸门

当前`AgentCore`本身已经明确：写入类工具必须由业务侧增加人工确认。这一原则应继续保持。

建议不要让AI直接执行“修改租金”“重写全篇”，而是拆成两步：

1. 提案工具只生成修改计划和预览；
2. 用户点击“确认应用”以后，再调用写入工具。

建议的提案/执行工具包括：

- `propose_parameter_change`；
- `propose_section_revision`；
- `propose_bulk_revision`；
- `apply_approved_calc_change`；
- `apply_approved_text_patch`；
- `run_targeted_review`；
- `export_selected_version`。

每个写入动作都应具备：操作编号、防重复执行、修改前快照、可撤销和日志。

### 5.4 哪些办公助手能力不应直接照搬

- 通用翻译、配图、播报等能力不是可研主线，暂不需要加入；
- 用户长期记忆只能保存文风和操作偏好，不能把某个项目的租金或行业参数当成永久事实；
- AI不能因为记住了用户习惯，就跳过公司规则或参数确认；
- AI不能自行计算财务指标，必须调用白箱测算工具；
- AI不能在用户没有确认时改动项目事实、测算参数或正式报告。

---

## 六、建议建设的目标产品形态

建议把当前产品从“多个功能页面”进一步升级为“项目工作空间”。

一个项目应统一包含以下对象：

| 对象 | 说明 |
|---|---|
| 项目事实版本 | 名称、地址、规模、类型、建设内容等 |
| 测算参数版本 | 参数值、单位、来源、依据、人工确认人和时间 |
| 测算结果快照 | 与某一版参数绑定的完整白箱计算结果 |
| 报告版本 | 正文、表格、人工编辑、锁定状态和生成时间 |
| 证据清单 | 每节使用的政策、项目资料、案例和Excel单元格 |
| 审查批次 | 规则版本、问题、修复状态和复核结果 |
| 对话任务 | 用户指令、AI理解、工具调用、待确认动作和执行结果 |
| 导出成果 | 明确对应哪版参数、正文、资料和审查结果 |

项目首页不需要复杂，最重要的是让用户一眼看到：

- 当前正式版本；
- 当前测算是否与正文一致；
- 有多少小节待更新；
- 有多少依据已过期；
- 有多少审查问题未解决；
- 上次工作做到哪里；
- 下一步建议做什么。

---

## 七、值得更新的功能清单与优先级

### P0：必须先做，直接解决“生成后不好改”

#### 1. 项目级AI对话与完整恢复

- AI可研会话由“每人覆盖一份”改为“每个项目可有多次会话”；
- 保存当前流程节点、待确认动作、生成任务和最后操作；
- 刷新或重新登录后显示“继续上次工作”；
- 报告完成后继续对话时直接进入当前项目，而不是重新抽取信息。

#### 2. 参数—指标—章节影响关系表

建立明确依赖关系：

> 测算参数 → 白箱计算结果 → 报告表格/指标 → 相关章节 → 审查规则

第一版不必追求AI自动学习，可以由现有测算公式、章节标签和业务规则建立确定映射，后续再持续补充。

#### 3. 测算修改预演

- 自然语言识别参数；
- 显示中文参数名、原值、新值、单位和来源；
- 调用异常检测和行业规则；
- 白箱引擎输出变化前后对比；
- 用户确认后才写入。

#### 4. 受影响章节标记与选择性更新

章节增加状态：

- 最新；
- 测算已变化，待同步；
- 依据已变化，待复核；
- 人工锁定；
- 有未解决审查问题。

用户可以只更新受影响的章节，不必重新生成整篇。

#### 5. 文字差异、接受/拒绝、撤销

- AI修改前保存快照；
- 显示删除、增加和替换内容；
- 支持逐节接受、拒绝、全部接受；
- 接受以后仍可撤销；
- 人工锁定内容不得被批量覆盖。

#### 6. 报告版本与测算快照绑定

每个报告版本必须记录：

- 使用哪版项目事实；
- 使用哪版测算参数和结果；
- 使用哪批正式资料；
- 使用哪版审查规则；
- 是否仍存在未解决问题。

### P1：在P0稳定后完成，提升可信度和效率

#### 7. 章节证据侧栏

- 显示该节使用的资料和具体定位；
- 支持预览来源；
- 支持固定/取消固定依据；
- 资料换版时标记受影响章节；
- 导出时可选择形成脚注、尾注或“主要依据表”。

#### 8. 审查问题一键形成修复提案

- 问题定位到具体文字、参数或依据；
- AI形成候选修改；
- 用户查看差异并确认；
- 只复核受影响问题，不必每次全篇重审；
- 保留“问题—修复—复核通过”的审计链。

#### 9. 固定流程时间线和可恢复长任务

- 展示资料、参数、测算、生成、审查、导出的进度；
- 逐节任务由服务端保存状态；
- 浏览器关闭后任务不中断；
- 失败章节可单独重试；
- 用户能看到失败原因和输入依据。

#### 10. 可研专用快捷动作

- 与最新测算一致；
- 补充正式依据；
- 修复本节审查问题；
- 保留数字，仅优化表达；
- 改为稳健/正式文风；
- 删除重复表述；
- 扩写或压缩；
- 锁定本节，不参与批量更新。

#### 11. 发布前就绪检查

导出正式版前统一检查：

- 测算与正文是否同版；
- 是否存在待更新章节；
- 是否使用失效资料；
- 是否存在未确认参数；
- 是否存在审查错误；
- 人工修改后是否复核；
- 导出文件是否绑定完整版本信息。

### P2：有真实项目验证后再决定

- 多人协作、批注、分工和审批；
- 两个项目或两个方案的深度对比；
- 企业级模板市场；
- 更复杂的外部数据MCP连接；
- 跨项目经验自动总结；
- 实时多人共同编辑。

### 暂时不建议做

- 自己复刻Dify式的自由拖拽工作流画布；
- 为了“看起来智能”引入大量多Agent角色；
- 在缺少案例数据时继续堆随机森林等黑箱预测；
- 用AI替换白箱测算；
- 重做一个与现有系统平行的RAG平台；
- 一次性添加大量与可研无关的通用办公工具。

---

## 八、RAG、Wiki、MCP在下一阶段分别扮演什么角色

### RAG：负责“找得到正确依据”

现阶段重点不再是单纯提高文件数量，而是：

- 让每个章节看得到证据；
- 让旧证据能被发现；
- 让资料更新能触发影响检查；
- 让引用结果进入正式报告追溯链。

### Wiki：负责“把稳定业务知识整理成人能维护的规则”

适合放置：

- 公司编制要求；
- 项目类型和地区适用口径；
- 参数解释和使用条件；
- 审查逻辑；
- 常见问题和成熟写法。

Wiki不应直接充当测算数据库，也不能替代正式资料的效力和版本管理。

### MCP：负责“以后连接外部系统”，不是当前主线

如果将来需要接入：

- 中指、Wind或其他授权数据接口；
- 企业OA、档案系统、SharePoint或网盘；
- 项目管理、合同、审批或ERP系统；
- 独立的地图、人口、企业和市场数据服务；

MCP会很有用，因为它可以用统一方式把外部系统暴露成工具。

但当前系统内部的测算、RAG、项目库和审查都在同一个应用里，已经有`AgentCore`工具注册机制。短期为内部功能再套一层MCP，收益不大，反而增加部署和排错成本。

建议顺序是：

1. 先用现有工具框架完成项目内闭环；
2. 把工具接口和权限边界稳定下来；
3. 真正需要接外部系统时，再将相应工具包装成MCP服务。

---

## 九、建议实施顺序

### 第一阶段：先完成最小闭环

1. 项目级会话和任务状态；
2. 测算参数版本与结果快照；
3. 参数影响关系表；
4. 参数修改预演；
5. 受影响章节标记；
6. 选择性更新；
7. 文字差异与接受/拒绝；
8. 报告版本回退；
9. 局部复核；
10. 绑定版本后导出。

第一阶段完成后，用户就可以真正做到：

> “先生成一版，然后通过对话不断改测算、改正文、补资料，系统只更新该更新的部分，而且每一步不会把原稿弄丢。”

### 第二阶段：把已有知识治理能力接到正文

1. 章节证据侧栏；
2. 正式资料换版影响提示；
3. Excel单元格来源可视化；
4. 引用固定和刷新；
5. 审查问题修复闭环；
6. 发布前就绪检查。

### 第三阶段：用真实项目决定深化方向

连续放入若干真实项目，记录：

- 用户最常改哪些参数；
- 哪些章节最常被重写；
- 哪些RAG证据最常误命中；
- 哪些审查问题重复出现；
- 哪些操作仍需要离开系统到Word/Excel完成。

之后再决定是否建设多人协作、外部MCP、更多自动化和跨项目学习。这样比凭想象继续堆功能更稳妥。

---

## 十、第一阶段的验收标准

下面这些不是宣传口号，而是开发完成后必须逐项实测的场景。

### 10.1 恢复与继续

- 生成到一半关闭页面，再次打开可从未完成章节继续；
- 报告完成后刷新，继续说“把租金改为42”，系统知道当前项目和当前值；
- 同一用户的两个项目拥有独立对话和状态，不会互相覆盖。

### 10.2 修改测算参数

- 用户使用中文参数名也能正确识别；
- 确认前数据库和当前正式参数不发生变化；
- 预演结果来自确定性测算引擎；
- 异常值能被提示或阻断；
- 确认后产生新测算版本；
- 受影响指标和章节清单准确。

### 10.3 修改正文

- 只更新受影响章节；
- 人工锁定章节不被覆盖；
- AI候选稿能查看差异；
- 拒绝修改后原文不变；
- 接受后可以撤销；
- 修改测算后，旧正文不会继续显示为“最新”。

### 10.4 审查与导出

- 只对修改过的章节运行局部审查；
- 修复后问题状态能闭环；
- 导出时能知道对应的参数版、报告版和资料版；
- 存在严重问题、失效证据或待同步章节时，系统明确警告；
- 旧版本仍能打开和导出，不会被新版本覆盖。

---

## 十一、建议对现有代码的落点

以下只是为了说明方案能够落地，不代表现在立即改代码。

| 现有位置 | 建议改造方向 |
|---|---|
| `aireport.js` | 把聊天从一次性流程升级为项目绑定的持续对话；保存可执行任务状态 |
| `functions/api/aireport.js` | 从每用户一份覆盖式存档改为项目/会话级存档 |
| `auth.js`、`functions/api/projects.js` | 项目中增加当前版本、任务状态和发布状态，不再只保存一个覆盖式JSON |
| `report.js` | 增加章节状态、证据、版本快照、差异候选稿和人工锁定 |
| `app.js` | 增加接受/拒绝/撤销、选择性更新和继续任务交互 |
| 白箱测算模块 | 增加“只预演不保存”的统一接口，以及新旧结果差异摘要 |
| `review.js` | 审查问题绑定版本、章节和修复状态，并支持局部复核 |
| `agent-core.js` | 继续作为统一工具引擎，保留写入必须人工确认的原则 |
| `agent-widget.js` | 复用现有只读工具，并按可研场景增加项目、版本、影响和证据工具 |
| RAG/Wiki/正式资料台账 | 输出章节级证据清单及资料换版影响，不重新建设底层知识库 |

建议先设计数据结构和状态转换，再改界面。否则容易出现“按钮很多，但版本和数据关系仍然混乱”的问题。

---

## 十二、最终成果应该是什么样

最终成果不应只是“AI帮我写了一份可研报告”，而应是：

> 每一个真实项目都有一个持续存在的工作空间。项目事实、测算参数、白箱结果、报告正文、知识依据、审查结果和对话任务互相绑定。用户可以随时修改参数或文字，系统先解释影响并预览，确认后只更新需要更新的部分，全过程保留版本、依据和操作记录，最后输出可追溯、可审查、可恢复的正式成果。

通俗地说，它应该像一个真正懂可研业务的项目助理：

- 它记得这是什么项目；
- 它知道数字是怎么算出来的；
- 它知道哪段话用了哪份资料；
- 它知道改一个数字会影响哪里；
- 它不会未经同意直接改正式数据；
- 它改完会告诉你改了什么；
- 它能把旧版本找回来；
- 它能在导出前提醒还有哪些风险。

这才是当前系统从“能生成报告”走向“能真正辅助完成可研编制和反复修改”的关键升级。

---

## 十三、最终建议

1. 现有RAG、Wiki、测算、审查和办公助手基础不需要推倒重来；
2. 下一轮开发优先做P0闭环，不要先做更多外围功能；
3. AI办公助手的工具框架值得复用，但可研写入动作必须经过预览和人工确认；
4. MCP暂时作为未来外部系统接入层，不要为了概念完整而提前复杂化；
5. 第一阶段完成后，立即用真实项目进行连续修改测试，而不是只验证“能不能生成第一版”；
6. 真正的衡量标准是：使用者能否在系统内完成第二版、第三版，且不需要反复回到Word和Excel手工核对。

